"""The `users` application layer — use cases, the ports they depend on,
and the command objects they accept. Imports `domain/` and platform
contracts from `core/`; never FastAPI, never SQLAlchemy.
"""
