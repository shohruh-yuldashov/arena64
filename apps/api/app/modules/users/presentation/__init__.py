"""The `users` presentation layer — transport bindings.

Named `presentation/` per this task; services.md §2.1 calls the same layer
`interface/`. See the module root docstring for that reconciliation.
"""
