"""`reference`'s domain — value types only, no aggregate.

Reference data has no lifecycle: nothing transitions a time control, and
there is no invariant spanning two rows. So there is no aggregate root here,
only frozen values and the closed enum that names them.
"""
