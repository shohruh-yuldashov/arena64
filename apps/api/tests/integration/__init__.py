"""Tests against real PostgreSQL, Redis, and a real broker
(services.md §1, tests/integration/) — empty until the first module gives
them something to run against. No real infrastructure exists in this
bootstrap; docker-compose service definitions are a follow-up (see this
task's recommendations)."""
