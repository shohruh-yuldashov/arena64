"""Full-stack tests across HTTP and WebSocket, over critical journeys
(services.md §1, CLAUDE.md §6 rule 3). Empty until there is a journey to
test — the first candidate is authentication once `auth` exists."""
