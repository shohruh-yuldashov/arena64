/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */

export interface paths {
    "/health": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Liveness */
        get: operations["liveness_health_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/health/ready": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Readiness */
        get: operations["readiness_health_ready_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/health": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Liveness */
        get: operations["liveness_api_v1_health_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/health/ready": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Readiness */
        get: operations["readiness_api_v1_health_ready_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/users/search": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Search for players
         * @description Finds players whose username or display name matches `q`.
         *
         *     Returns the **same** profile representation as
         *     `GET /profiles/{username}`, field for field, including the privacy
         *     behaviour: a hidden country, record or presence is `null` here exactly as
         *     it is there, and a `null` never says which of its reasons applies.
         *
         *     **Authenticated.** Unlike every other profile read on this platform, this
         *     one requires a token — see this module's docstring on why that is the
         *     enumeration control rather than an inconsistency.
         *
         *     ## Ordering
         *
         *     Ranked, in four buckets, then alphabetically inside each:
         *
         *     | Rank | Match |
         *     | --- | --- |
         *     | 1 | the username is exactly `q` |
         *     | 2 | the username starts with `q` |
         *     | 3 | the display name starts with `q` |
         *     | 4 | `q` appears anywhere in either |
         *
         *     Stable for a given term: the same query returns the same order, which is
         *     what makes the cursor correct rather than approximate.
         *
         *     ## Pagination
         *
         *     Keyset, never offset. Follow `page.next_cursor` until it is `null`;
         *     `page.has_more` says whether there is one. There is deliberately **no
         *     total**: counting a partial-match query costs as much as running it, and
         *     a count is not what somebody looking for a person needs.
         *
         *     ## What is never returned
         *
         *     Deactivated accounts, and yourself. Neither appears at any rank, under
         *     any term — the first because which handles belong to withdrawn accounts
         *     is a disclosure, the second because a search whose purpose is finding
         *     other people should not spend a row on the person doing the searching.
         *
         *     Email addresses, preferences and privacy settings do not appear here
         *     because they are not on the shape this returns; there is nothing to
         *     filter out.
         */
        get: operations["search_users_api_v1_users_search_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/users/{user_id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Resolve a player id to a public identity
         * @description Turns a `player_id` into a name and a picture.
         *
         *     Exists because every cross-context reference on this platform is an
         *     opaque `player_id` (DM-06), so a match card or a leaderboard row holds
         *     an id and needs a handle to render — and `GET /profiles/{username}`
         *     takes the wrong key.
         *
         *     **Carries no email, no account state and no storage key.** Until
         *     A64-012.6 it returned all three; see this module's docstring.
         *
         *     For a player's full public profile — bio, country, ratings, statistics,
         *     and the privacy settings that govern them — use
         *     `GET /profiles/{username}`.
         *
         *     `404` if no such user, raised as `UserNotFound` by the service and
         *     mapped by the platform handler, not here.
         */
        get: operations["get_user_api_v1_users__user_id__get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/users": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List players
         * @description Keyset-paginated listing (RP-03) — the cursor is opaque and must be
         *     passed back unchanged, never constructed by a client.
         *
         *     Returns the same `PublicUserResponse` as the single-user route above,
         *     which is what makes "what does this endpoint expose" one answer rather
         *     than two. Before A64-012.6 it returned `UserSummary` directly — no
         *     email, but the raw avatar `object_key` per row, which is a storage
         *     detail no client should see and no deployment should have to keep
         *     stable.
         *
         *     **Active accounts only.** A deactivated account does not appear here, in
         *     any page, under any parameter — see the A64-012.8 note in this module's
         *     docstring for why the `is_active` filter that used to select them was
         *     removed rather than defaulted.
         */
        get: operations["list_users_api_v1_users_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/auth/register": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Create an account
         * @description Registers a new account and returns it.
         *
         *     Returns `201` with a `Location` header pointing at the canonical
         *     resource — the created account is readable at `GET /api/v1/users/{id}`,
         *     and saying so is what `201` is for.
         *
         *     **No tokens are issued.** Registration proves you can fill in a form,
         *     not that you own the address; signing in is a separate call. Issuing a
         *     session here would also mean an unverified account holding a 30-day
         *     credential before A64-011.6 has had a chance to verify anything.
         *
         *     A verification link is sent to the address (A64-011.6). Delivery
         *     failure does not fail the request — the account exists and the person
         *     can ask for another link.
         *
         *     The body carries no `password_hash`: `UserRead` has no such field, so
         *     that is a property of the type rather than of remembering to exclude
         *     it. `is_verified` is `false` until the link is redeemed.
         */
        post: operations["register_api_v1_auth_register_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/auth/login": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Sign in and receive a token pair
         * @description Verifies credentials and starts a session.
         *
         *     `200`, not `201`: a session row is created, but the thing the caller
         *     asked for is a credential, not a resource with a URL.
         *
         *     Three services, in an order that matters: identity is proven first,
         *     and only then is anything issued. `AuthenticationService` raises
         *     before either token service is reached, so a failed sign-in creates no
         *     session row and mints no JWT.
         *
         *     A failed sign-in returns `401 invalid_credentials` whether the address
         *     is unknown or the password is wrong — identically, and in the same
         *     elapsed time. `403` means the credentials were right but the account
         *     is deactivated or temporarily locked.
         *
         *     Each sign-in starts an independent session, so signing in on a phone
         *     does not disturb a laptop.
         *
         *     **Marks the player online** (A64-013.6). Presence is best-effort and
         *     expires on its own timer, so a client that wants to keep showing as
         *     online must keep refreshing — see `POST /auth/refresh`. Nothing about
         *     presence can fail this request.
         */
        post: operations["login_api_v1_auth_login_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/auth/refresh": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Exchange a refresh token for a new token pair
         * @description Rotates the refresh token and issues a fresh access token.
         *
         *     **The presented refresh token is invalidated.** Every refresh returns
         *     a new one, and the old one stops working immediately — database.md
         *     §14.3's rotation-on-every-use. A client must store what it receives;
         *     retrying with the previous token is indistinguishable from a replay.
         *
         *     **Presenting an already-rotated token revokes the whole chain.** That
         *     is the point of rotating: a token used twice was captured, and since
         *     the platform cannot tell the attacker from the legitimate user, both
         *     are signed out of that session. Expect `401 invalid_session`, and
         *     expect the successor to stop working too.
         *
         *     `401 session_expired` means the session aged out — through the 30-day
         *     absolute window or the idle window — and the caller must sign in
         *     again. Every other failure is `401 invalid_session`: unknown token,
         *     revoked session, malformed value. They are deliberately
         *     indistinguishable.
         *
         *     The account is re-read here rather than trusted from the session,
         *     because the access token must reflect the account as it is *now* — and
         *     because a deactivated or deleted account must not be able to refresh
         *     its way to a fresh 15 minutes of access.
         *
         *     **Marks the player online and restarts their presence window**
         *     (A64-013.6). This is what keeps a signed-in player visible as online
         *     without a socket: a client that stops refreshing stops being online one
         *     `PRESENCE_TTL_SECONDS` later, with nothing having to observe that it
         *     left. A client that wants to stay online should refresh well inside
         *     that window.
         */
        post: operations["refresh_api_v1_auth_refresh_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/auth/logout": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Sign out of the current device
         * @description Revokes the session the refresh token belongs to.
         *
         *     Takes the **refresh token**, not the access token, and that is the
         *     substantive choice in this endpoint. An access token names a *user*,
         *     not a session — it has no `sid` claim — so it cannot say which of five
         *     devices to sign out. The refresh token names exactly one session,
         *     which is precisely what "sign out of this device" means.
         *
         *     `204` with no body: there is nothing to say, and a body would invite a
         *     client to parse it.
         *
         *     **Idempotent.** Signing out twice succeeds twice. A caller retrying
         *     after a dropped response must not receive an error for the retry, and
         *     "the session is already gone" is the outcome it wanted.
         *
         *     Deliberately does *not* invalidate the access token — nothing can, for
         *     up to its remaining 15 minutes. That window is the documented cost of
         *     a stateless token (`JWTSettings`), and closing it needs the `jti`
         *     denylist recommended for A64-011.6.
         *
         *     **Also deliberately does not mark the player offline** (A64-013.6).
         *     Presence is per *player*, not per session, and somebody signing out on
         *     a laptop may still be signed in on a phone — publishing "offline" here
         *     would be a falsehood the phone's next refresh silently corrects.
         *     `POST /auth/logout-all` is the one that means it; short of that, the
         *     presence window expiring is what marks an absent player absent.
         */
        post: operations["logout_api_v1_auth_logout_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/auth/logout-all": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Sign out of every device
         * @description Revokes every session for the authenticated account.
         *
         *     Takes the **access token** rather than a refresh token, unlike
         *     `/logout`, and the asymmetry is deliberate: this operates on the
         *     account, not on one device, so the credential that names an account is
         *     the right one. It also means a player whose laptop was stolen can sign
         *     out everywhere from their phone without holding the laptop's token.
         *
         *     Revokes the calling session too. "Log out everywhere" that quietly
         *     excluded the device you asked from is not what anyone means by it —
         *     the SE-1 exception exists for password changes, where staying signed
         *     in is the point, and this is not that.
         *
         *     `204`, and idempotent: revoking nothing succeeds.
         *
         *     **Marks the player offline** (A64-013.6) — the only endpoint that does,
         *     because it is the only one that leaves no device able to be present.
         *     `POST /auth/logout` deliberately does not; see its description.
         */
        post: operations["logout_all_api_v1_auth_logout_all_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/auth/ws-ticket": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Mint a single-use WebSocket ticket
         * @description Issues the credential `GET /ws` accepts — AD-09.
         *
         *     An ordinary authenticated route, and that is the design rather than an
         *     implementation detail: browsers cannot set headers on a WebSocket
         *     handshake, so the socket's credential has to survive a query string —
         *     and the only safe thing to put there is a value that is worthless
         *     seconds later and cannot be replayed.
         *
         *     **This is not a second authentication mechanism.** The caller has
         *     already been authenticated by `TokenValidator` in the ordinary way
         *     (`CurrentUser`); a ticket is downstream of that check rather than
         *     alongside it, so there remains exactly one thing on this platform that
         *     decides whether a credential is valid.
         *
         *     `201`, because a ticket is a resource this call brings into existence
         *     and each call makes a different one. No `Location` header: the ticket
         *     is not addressable, which is the point of it.
         *
         *     Not throttled by its own rule. A client legitimately mints one per
         *     socket and reconnects on a flaky network; the platform-wide limit is
         *     what bounds abuse, and a second policy here would be a second place to
         *     get one number right.
         */
        post: operations["websocket_ticket_api_v1_auth_ws_ticket_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/auth/me": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Read the authenticated account
         * @description Returns the account the access token speaks for.
         *
         *     The identity comes from the token's `sub` — no database read is needed
         *     to know *who* is asking. The read here is for the profile, which the
         *     token deliberately does not carry: claims are base64 and end up in
         *     `localStorage` and proxy logs, and a handle copied into a 15-minute
         *     credential is a copy that can be wrong.
         *
         *     Includes `email`, because this endpoint is scoped to the caller's own
         *     account by construction — `sub` cannot name anyone else.
         *
         *     Returns `404` if the account was deleted while a valid token was still
         *     in flight. That is a real, if narrow, window: the token stays
         *     cryptographically valid for its remaining minutes regardless.
         */
        get: operations["me_api_v1_auth_me_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/auth/email/verify": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Confirm an email address
         * @description Redeems a verification link.
         *
         *     The `token` is the query parameter from the link in the email. The
         *     link points at a frontend page; that page posts the value here rather
         *     than the API reading it from a query string, so the token stays out of
         *     access logs and browser history on this side.
         *
         *     **One-time use.** A redeemed token is dead — clicking the same link
         *     twice returns `422`, not a second success. Every other outstanding
         *     link for the account dies with it, so a resend chain cannot leave a
         *     stale link alive behind a used one.
         *
         *     `422 invalid_verification_token` covers every failure: unknown token,
         *     already redeemed, expired. They are deliberately indistinguishable —
         *     the client's action is the same in all three (offer a new link), and
         *     telling a caller which it was reports on whether a token they hold was
         *     ever real.
         *
         *     Not `401`: this endpoint is not about identity and needs no
         *     authentication. A `401` would send someone who clicked a stale link to
         *     a sign-in form, which does not fix anything.
         *
         *     Returns the account with `is_verified` now `true`. Idempotent at the
         *     account level — an account that was somehow already verified is
         *     returned unchanged rather than erroring.
         */
        post: operations["verify_email_api_v1_auth_email_verify_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/auth/email/resend": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Request a new verification link
         * @description Sends a fresh verification link, if there is anything to send.
         *
         *     **The response is identical in every case** — unknown address,
         *     already-verified account, or a link genuinely sent. That is the
         *     security property, not an implementation shortcut: this endpoint is
         *     unauthenticated by necessity (the person who needs it never received
         *     the first link and may never have signed in), which makes it an
         *     account-enumeration surface unless it says nothing.
         *
         *     So it says nothing. No status code, body, or timing distinguishes the
         *     three outcomes, and the reply is the deliberately vague "if an account
         *     exists for that address, a verification link has been sent".
         *
         *     `202 Accepted`, not `200`: the work is handed to a mail provider and
         *     the outcome is not known when this returns. `202` is the honest code
         *     for "we have taken this and will act on it".
         *
         *     Issuing a new link **invalidates the previous one** — database.md §4.5
         *     keeps at most one live token per account, enforced by a partial unique
         *     index. A user who clicks an older link after requesting a new one gets
         *     `422` and should use the newest email.
         *
         *     A malformed address is `422`, which reveals nothing: an address that
         *     cannot be valid cannot belong to anyone.
         */
        post: operations["resend_verification_api_v1_auth_email_resend_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/auth/password/forgot": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Request a password reset link
         * @description Sends a password reset link, if there is anything to send.
         *
         *     **The response is identical in every case** — unknown address,
         *     deactivated account, or a link genuinely sent. That is the security
         *     property, not an implementation shortcut. This endpoint is
         *     unauthenticated by necessity (the person who needs it cannot sign in),
         *     which makes it an account-enumeration surface unless it says nothing,
         *     and the thing it would disclose is more valuable than most: an
         *     attacker probing here is deciding which addresses are worth a phishing
         *     campaign that ends in a password.
         *
         *     So it says nothing. No status code and no body distinguishes the three
         *     outcomes. `PasswordResetService` is built so this handler *cannot*
         *     leak — `forgot_password` returns `None` in every case, so there is
         *     nothing here to branch on.
         *
         *     `204 No Content` rather than the `202 Accepted` its sibling
         *     `/auth/email/resend` returns, and the difference is deliberate rather
         *     than an inconsistency. `202` carries a body explaining that a link
         *     *may* have been sent, which is useful when the client wants a sentence
         *     to render. Here there must be nothing to render that a client could
         *     accidentally make conditional, and `204` guarantees that by having no
         *     body at all. The screen after this endpoint says "if an account exists
         *     for that address, we have sent a link" whatever happened — and that
         *     sentence belongs to the frontend, which does not need the server's
         *     permission to display it.
         *
         *     Issuing a new link **invalidates the previous one** — database.md §4.5
         *     keeps at most one live token per account, enforced by a partial unique
         *     index. Somebody who asks twice and then clicks the older email gets
         *     `422` and should use the newest one.
         *
         *     A malformed address is `422`, which reveals nothing: an address that
         *     cannot be valid cannot belong to anyone.
         *
         *     **Not rate limited.** Nothing here stops an attacker from calling it
         *     ten thousand times, which is both a mail-bombing vector aimed at one
         *     person's inbox and the enumeration probe the identical response exists
         *     to frustrate. That is A64-011.8's task and it is the first
         *     recommendation this one makes.
         */
        post: operations["forgot_password_api_v1_auth_password_forgot_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/auth/password/reset": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Set a new password using a reset link
         * @description Redeems a password reset link and sets a new password.
         *
         *     The `token` is the query parameter from the link in the email. The link
         *     points at a frontend page; that page collects the new password and
         *     posts both here, rather than the API reading the token from a query
         *     string, so a credential that replaces passwords stays out of access
         *     logs and browser history on this side.
         *
         *     **One-time use.** A redeemed token is dead — submitting the same link
         *     twice returns `422`, not a second success. Every other outstanding
         *     reset link for the account dies with it.
         *
         *     **Every session is revoked, including on devices that did nothing
         *     wrong.** Refresh tokens stop working, and the person must sign in again
         *     everywhere with the new password. That is the point rather than a side
         *     effect: the plausible reason somebody is resetting a password is that
         *     somebody else knows the old one, and a reset that left the attacker's
         *     session alive would have achieved nothing.
         *
         *     Access tokens already issued keep working for up to their remaining 15
         *     minutes — nothing can revoke a stateless JWT, which is the documented
         *     cost recorded on `JWTSettings` and the reason the window is short.
         *     Closing it needs the `jti` denylist recommended since A64-011.6.
         *
         *     `204` and no body. Deliberately no token pair: this endpoint has
         *     verified control of an *inbox*, not knowledge of a password, and
         *     handing back a live session would make an email-account compromise
         *     silently equivalent to a sign-in. The client's next call is
         *     `POST /auth/login`.
         *
         *     `422 invalid_reset_token` covers every token failure: unknown, already
         *     redeemed, expired. They are deliberately indistinguishable — the
         *     client's action is the same in all three (ask for a new link), and
         *     telling a caller which it was reports on whether a token they hold was
         *     ever real.
         *
         *     `422 weak_password` means the new password failed the policy, and it is
         *     returned **whether or not the token was any good** — the schema checks
         *     the password while parsing, before the token is ever looked up. That
         *     ordering is what stops this endpoint from becoming a free token
         *     oracle; see `PasswordResetService`.
         *
         *     Not `401` in any case: this endpoint is not about identity and needs no
         *     authentication. A `401` would send somebody who clicked a stale link to
         *     a sign-in form, which is exactly what they cannot do.
         */
        post: operations["reset_password_api_v1_auth_password_reset_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/profiles/{username}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Read a player's public profile
         * @description Returns the public profile of the player holding `username`.
         *
         *     **Case-insensitive.** `/profiles/Alice`, `/profiles/alice` and
         *     `/profiles/ALICE` resolve to one account, because usernames are unique
         *     on their case-folded form (domain-model.md UP-1). The `username` in the
         *     response is the casing that player chose, which is what a client should
         *     render.
         *
         *     **Public, and optionally authenticated.** No token is required. If you
         *     present one, the response is composed against your *relationship* to
         *     this player: a friend sees fields restricted to friends, and somebody
         *     either party has blocked sees none of the audience-valued fields at all.
         *
         *     An anonymous read is always the most restrictive view. Nothing here is
         *     gated behind authentication itself — the body carries no email, no
         *     account state and no activity data whoever asks.
         *
         *     `404` when there is no visible profile — whether the username was never
         *     registered or the account has been deactivated. The two are
         *     deliberately indistinguishable, so that this endpoint cannot be used to
         *     discover which handles belong to withdrawn accounts, which is what an
         *     impersonator would want before adopting one.
         *
         *     ## Presence: `is_online` and `last_seen`
         *
         *     Both are best-effort and both may be `null`, and a `null` deliberately
         *     means **nothing more than "nothing can be said"**. It covers a player
         *     who has hidden their presence, a player nobody has observed, a presence
         *     record that has expired, and presence being temporarily unavailable —
         *     all rendered identically, because reporting which applies would answer
         *     the question the privacy setting exists to decline.
         *
         *     Read them independently. They are governed by two different settings
         *     with two different defaults, and the common case is a player who shows
         *     `is_online` and withholds `last_seen`: `show_last_seen` is the one
         *     privacy flag that is off out of the box, because a published "last seen
         *     03:14" is a sleep schedule while "online now" is momentary.
         *
         *     Render `null` as *unknown*, never as *offline* — `is_online: false` is
         *     the value that means offline, and it is only available for a player seen
         *     disconnecting recently.
         *
         *     **Both are real since A64-013.6**, which gave presence a producer:
         *     signing in and refreshing a token mark a player online, and signing out
         *     of every device marks them offline. What a *viewer* sees is still gated
         *     by `online_status` and `show_last_seen` — so `null` here means hidden,
         *     unobserved, lapsed or unavailable, and never says which.
         *
         *     `bio` and `country` are `null` only when a player has not set them — or,
         *     for `country`, when they have chosen not to show it, which is
         *     indistinguishable here for the same reason presence is. Both are written
         *     through `PATCH /profile`.
         *
         *     ## Ratings and statistics are real fields with placeholder values
         *
         *     Every player currently reports the starting rating in each category,
         *     marked `is_provisional: true`, with every match count at zero. That is
         *     the truthful answer while no match has ever been played, and it is the
         *     same shape the rating system will return — so a client written today
         *     needs no change when real values arrive.
         *
         *     **Read `is_provisional` before rendering a rating.** A provisional
         *     rating is a starting value rather than a measurement, and displaying
         *     one unmarked misleads both the viewer and any opponent deciding whether
         *     to accept a challenge (domain-model.md PR-6).
         */
        get: operations["get_profile_api_v1_profiles__username__get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/profile/me": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Read your own profile
         * @description Returns the authenticated account's own profile.
         *
         *     Differs from `GET /profiles/{username}` in two ways that matter. It is
         *     **never redacted** — statistics and presence are reported whatever the
         *     privacy flags say, because those govern strangers rather than you; and
         *     it is scoped to the caller by construction, so it needs no username and
         *     cannot be pointed at anybody else.
         *
         *     Differs from `GET /auth/me` in the other direction: that one answers
         *     *who am I* and carries the email and verification state; this one
         *     answers *what does my profile say* and carries the three editable
         *     fields. Together they are the two halves an account settings page
         *     needs.
         *
         *     **No `timezone` and no `language`.** Both were here until A64-012.5
         *     moved them to `GET /profile/preferences`, which is now the only place
         *     either is read or written on this router.
         *
         *     Carries your **statistics** and your **presence**, always —
         *     `show_statistics`, `show_online_status` and `show_last_seen` govern what
         *     a stranger sees on `GET /profiles/{username}`, never what you see of
         *     yourself (A64-012.6, A64-012.7). A control that hid a record from the
         *     person who hid it would be one nobody could verify they had set.
         *
         *     Since A64-013.6 presence is recorded by authentication itself — signing
         *     in and refreshing a token mark a player online — so a caller reading this
         *     endpoint normally sees themselves online. `null` means unobserved (a
         *     lapsed presence window, or `PRESENCE_ENABLED=false`), never hidden.
         *
         *     Returns exactly the shape `PATCH /profile` returns, so a client can
         *     populate an edit form and render the result of a save with one parser.
         */
        get: operations["get_my_profile_api_v1_profile_me_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/profile": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        /**
         * Update your own profile
         * @description Applies a partial update to the authenticated account's profile.
         *
         *     **A real PATCH.** Send only what changes; omitted fields are left
         *     alone. All three fields — `display_name`, `bio`, `country` —
         *     distinguish *omitted* from *explicitly null*: omitting leaves the value
         *     as it is, sending `null` clears it.
         *
         *     **Only your own profile.** The account comes from your access token;
         *     there is no way to name a different one.
         *
         *     **Unknown fields are rejected.** A body carrying `username`,
         *     `is_verified` or `avatar_object_key` returns `422` naming the field.
         *     None of them is settable here, and failing loudly is what stops a
         *     client from believing a rename succeeded.
         *
         *     ## Validation
         *
         *     | Field | Rule |
         *     | --- | --- |
         *     | `display_name` | 3-50 characters, any script, trimmed |
         *     | `bio` | at most 500 characters, plain text |
         *     | `country` | an assigned ISO 3166-1 alpha-2 code |
         *
         *     `preferred_language` and `timezone` were editable here until A64-012.5
         *     moved them to `PATCH /profile/preferences`. Sending either now returns
         *     a `422` naming it, which is the loud answer a silently-dropped setting
         *     would not give.
         *
         *     `display_name` and `bio` additionally reject control and
         *     bidirectional characters — a right-to-left override in a name rendered
         *     beside every match reverses the text around it, which is a display
         *     spoofing primitive rather than a typo.
         *
         *     **Nothing is written if any field is rejected.** Values are validated
         *     as they are converted, before the account row is touched, so a request
         *     with a good bio and a bad country changes neither.
         *
         *     Returns the full profile with **normalised** values — a padded display
         *     name comes back trimmed, `uz` comes back `UZ` — so a client renders
         *     what was actually stored rather than what it sent.
         */
        patch: operations["update_my_profile_api_v1_profile_patch"];
        trace?: never;
    };
    "/api/v1/profile/privacy": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Read your privacy settings
         * @description Returns the authenticated account's privacy settings.
         *
         *     Always all five flags, never a subset. An account has an answer for
         *     every one of them — the columns are `NOT NULL` and the platform
         *     defaults apply to rows nobody has touched — so a client renders a
         *     settings screen from this response alone and never has to guess.
         *
         *     Deliberately **not** rate limited: it is one indexed read of a row you
         *     have already authenticated as, it changes nothing, and a settings
         *     screen loads it on every visit. See `rate_limits.py`.
         *
         *     Scoped to you by construction. There is no path segment or parameter
         *     that could name another account, which is why there is no ownership
         *     check — and why no endpoint anywhere returns *somebody else's* flags.
         *     A stranger's profile shows a `null` where a hidden field would be and
         *     says nothing about why.
         */
        get: operations["get_my_privacy_settings_api_v1_profile_privacy_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        /**
         * Update your privacy settings
         * @description Applies a partial update to the authenticated account's privacy
         *     settings.
         *
         *     **A real PATCH.** Send only the flags you are changing; the rest are
         *     left exactly as they were. `{"show_country": false}` does not reset the
         *     other four to their defaults — which matters most for `show_last_seen`,
         *     whose default is *off*, so a reset would publish something the player
         *     had deliberately kept private.
         *
         *     **Only your own settings.** The account comes from your access token;
         *     there is no way to name a different one.
         *
         *     **Unknown fields are rejected**, and a flag sent as `null` is rejected
         *     too. A boolean control has two states, so `null` has no meaning here
         *     that would not have to be invented — and the most likely invention,
         *     "reset to default", is the one thing this endpoint must not do
         *     implicitly.
         *
         *     ## What each flag governs
         *
         *     | Flag | Effect on your public profile | Default |
         *     | --- | --- | --- |
         *     | `show_country` | `country` is `null` when off | `true` |
         *     | `show_last_seen` | `last_seen` is `null` when off | **`false`** |
         *     | `show_statistics` | `statistics` is `null` when off — never zeroes | `true` |
         *     | `show_online_status` | live presence, once it exists | `true` |
         *     | `show_activity` | recent activity, once it exists | `true` |
         *
         *     A hidden field is `null` and nothing else: no placeholder, no marker,
         *     and nothing that distinguishes it from a field you simply never filled
         *     in. That is deliberate — reporting *that* something is hidden answers
         *     the question hiding it was meant to decline.
         *
         *     **Ratings are always public** and no flag covers them. A rating is what
         *     pairing is computed from and what leaderboards publish; `show_statistics`
         *     covers the record of games behind it, not the number itself.
         *
         *     Rate limited per network address. See the `429` response.
         */
        patch: operations["update_my_privacy_settings_api_v1_profile_privacy_patch"];
        trace?: never;
    };
    "/api/v1/profile/preferences": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Read your preferences
         * @description Returns the authenticated account's personal settings.
         *
         *     **Always complete.** Every group is present and every setting inside it
         *     has a value, even for an account that has never opened a settings
         *     screen — the stored document is empty for such an account and the
         *     domain fills each absent key with its default. So a client renders
         *     every control from this response and never has to carry its own copy of
         *     the defaults, which is the copy that would drift.
         *
         *     Not rate limited: one indexed read of a row you have already
         *     authenticated as, changing nothing, loaded on every visit to a settings
         *     screen. See `rate_limits.py`.
         *
         *     **Never public.** No anonymous endpoint returns any of this — a board
         *     theme and a timezone are yours, and `GET /profiles/{username}` carries
         *     neither.
         */
        get: operations["get_my_preferences_api_v1_profile_preferences_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        /**
         * Update your preferences
         * @description Applies a partial update to the authenticated account's preferences.
         *
         *     **A real PATCH, at two levels.** An omitted *group* is left entirely
         *     alone, and inside a group an omitted *setting* is left alone. So
         *     `{"locale": {"timezone": "UTC"}}` changes a timezone and touches
         *     neither the language beside it nor any of the five gameplay settings.
         *
         *     **Only your own preferences.** The account comes from your access
         *     token; there is no way to name a different one.
         *
         *     **Unknown keys are rejected at both levels.** `{"ui": {...}}` is a
         *     `422` naming `ui`; `{"gameplay": {"sound": true}}` is a `422` naming
         *     `sound`. A `null` value is rejected too — no preference has an empty
         *     state, so `null` would need a meaning invented for it, and the likely
         *     invention ("reset to default") is not a decision this endpoint should
         *     make on a client's behalf.
         *
         *     ## Groups and settings
         *
         *     | Group | Setting | Values | Default |
         *     | --- | --- | --- | --- |
         *     | `gameplay` | `board_theme` | `classic`, `wood`, `marble`, `midnight` | `classic` |
         *     | `gameplay` | `piece_set` | `classic`, `modern`, `neo` | `classic` |
         *     | `gameplay` | `confirm_move` | boolean | `false` |
         *     | `gameplay` | `show_coordinates` | boolean | `true` |
         *     | `gameplay` | `animation_speed` | `instant`, `fast`, `normal`, `slow` | `normal` |
         *     | `locale` | `preferred_language` | `en`, `ru`, `uz` | `en` |
         *     | `locale` | `timezone` | any IANA name | `UTC` |
         *
         *     `animation_speed: instant` disables motion rather than being a fourth
         *     speed — it is an accessibility setting, and motion is a migraine and
         *     vestibular trigger.
         *
         *     **`preferred_language` and `timezone` are changed here and nowhere
         *     else.** Both were editable through `PATCH /profile` before A64-012.5,
         *     which removed them there; a field with two writable endpoints is a
         *     field whose validation and audit trail depend on which one a client
         *     happened to use.
         *
         *     **Nothing is written if any value is rejected.** The timezone is
         *     validated before the account row is touched, so a request with a good
         *     board theme and a bad timezone changes neither.
         *
         *     Rate limited **per account** rather than per network address, so a
         *     shared office or carrier connection never throttles one player for
         *     another's behaviour. See the `429` response.
         */
        patch: operations["update_my_preferences_api_v1_profile_preferences_patch"];
        trace?: never;
    };
    "/api/v1/profile/avatar": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Read your avatar's metadata
         * @description Returns the authenticated account's avatar metadata.
         *
         *     `404` when the account has no avatar. That is deliberately different
         *     from `DELETE`, which succeeds in the same state: this endpoint asks for
         *     a resource, and answering `200` with a body of nulls would make a
         *     client inspect three fields to learn what a status code says in one.
         *
         *     `dimensions` is **omitted** here. Reporting it would mean fetching and
         *     decoding the stored file on every read, which is a decode per profile
         *     page to report two numbers the client can read off the image it is
         *     about to load anyway. `POST` returns them because the encode that
         *     produced them just happened.
         *
         *     Returns only *this* account's avatar. Anyone else's is on
         *     `GET /api/v1/profiles/{username}`, which is public and needs no token.
         */
        get: operations["get_avatar_api_v1_profile_avatar_get"];
        put?: never;
        /**
         * Upload or replace your avatar
         * @description Stores a new avatar for the authenticated account, replacing any
         *     existing one.
         *
         *     **Upload and replace are the same call.** There is no separate replace
         *     endpoint: sending a new image when one already exists replaces it and
         *     removes the previous objects. A client does not need to know which case
         *     it is in, and could not usefully act on the difference.
         *
         *     `200`, not `201`. The avatar is a property of an account that already
         *     exists rather than a new resource with its own URL, and there is no
         *     `Location` a client could follow to it — the URLs are in the body.
         *
         *     ## What happens to the file
         *
         *     Nothing is stored until the image has been validated and re-encoded.
         *     In order: the size is checked, then the **file signature** — the
         *     declared `Content-Type` is ignored entirely, because it is a
         *     client-supplied string and a renamed executable sets it to `image/png`
         *     as easily as a real image does. Only then is the file decoded.
         *
         *     The stored image is **not the file that was uploaded**. It is
         *     re-encoded to WebP from decoded pixels, which strips every piece of
         *     metadata the original carried — EXIF on a phone photo routinely
         *     includes GPS coordinates and a device serial, and publishing an avatar
         *     must not publish where it was taken. Orientation is applied to the
         *     pixels first, so an image that relied on an EXIF rotation tag still
         *     appears the right way up in renderers that ignore the tag.
         *
         *     Two renditions are stored: the original fitted inside 512x512 and a
         *     thumbnail fitted inside 128x128. **Neither is cropped** — a non-square
         *     image is fitted inside the box and keeps its aspect ratio, so a 3:2
         *     photo becomes 512x341. An image already smaller than the box is not
         *     enlarged.
         *
         *     The original filename is discarded and never used: the stored object
         *     gets a cryptographically random name.
         *
         *     ## Caching
         *
         *     Both URLs carry a `?v=` matching `avatar_version`, which increments on
         *     every upload and every delete. The object key itself is random per
         *     upload, so the URLs of a replaced avatar are new either way — treat any
         *     avatar URL as immutable and cache it freely.
         */
        post: operations["upload_avatar_api_v1_profile_avatar_post"];
        /**
         * Delete your avatar
         * @description Removes the authenticated account's avatar — both stored renditions
         *     and the reference to them.
         *
         *     **Idempotent.** Deleting when there is no avatar succeeds, and
         *     `avatar_version` still increments. A caller retrying after a dropped
         *     response must not receive an error for the retry, and "there is no
         *     avatar" is the outcome it wanted.
         *
         *     `200` with a body rather than `204`, because there *is* something to
         *     say: the new `avatar_version`. A client that cached the previous URL
         *     needs it precisely now — the version is what tells any browser or CDN
         *     still holding the old image to stop serving it. A `204` would leave the
         *     client to guess, or to issue a second request for a number this one
         *     already computed.
         *
         *     `avatar_url` and `thumbnail_url` come back `null`. Rendering a
         *     placeholder — an identicon, a coloured initial — is the client's
         *     decision, because only the client knows the size and the surrounding
         *     design.
         */
        delete: operations["delete_avatar_api_v1_profile_avatar_delete"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/friends/requests": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Send a friend request
         * @description Sends a friend request from your account to `player_id`.
         *
         *     `201`, because a request is a new resource with an identifier the
         *     response carries — and the identifier is what every other endpoint here
         *     takes.
         *
         *     **The sender is your token, never a field.** There is no way to send a
         *     request as somebody else.
         *
         *     ## What is refused, and why
         *
         *     | Outcome | `code` | Rule |
         *     | --- | --- | --- |
         *     | `422` | `validation_error` | You addressed yourself |
         *     | `409` | `duplicate_friend_request` | You already have one pending to them (FR-1) |
         *     | `409` | `opposite_friend_request_pending` | They already have one pending to you |
         *
         *     The second `409` is the one worth handling specially: the right response
         *     is to show the request you already have and offer to accept it. It is
         *     **not** accepted automatically — two people each sending a request is
         *     not the same event as one agreeing to the other's, and converting it
         *     would resolve a request nobody acted on.
         *
         *     A request to a player who does not exist is not distinguished from one
         *     that does. Checking would mean a read whose *timing* answers "is there
         *     an account with this id", which on an endpoint taking an id is an
         *     existence oracle.
         */
        post: operations["send_friend_request_api_v1_friends_requests_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/friends/requests/incoming": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List friend requests you have received
         * @description Returns the pending requests **sent to you**, newest first.
         *
         *     `player` on each row is the *sender* — the person deciding whether to
         *     accept is the one reading this, so the profile shown is the other party's.
         *
         *     Only `pending` requests appear. A declined or cancelled request leaves
         *     this list silently and is not deleted: the row is history, and FR-5's
         *     future decline cooldown reads it.
         *
         *     **Keyset pagination, never offset.** Follow `page.next_cursor` until it
         *     is `null`. There is deliberately no total — counting costs as much as
         *     the page and is not what somebody triaging requests needs.
         */
        get: operations["list_incoming_api_v1_friends_requests_incoming_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/friends/requests/outgoing": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List friend requests you have sent
         * @description Returns the pending requests **you have sent**, newest first.
         *
         *     `player` on each row is the *recipient*.
         *
         *     A request that the other player declines simply disappears from here,
         *     with no notification and no explanation — FR-3, and it is deliberate: a
         *     notified decline turns a refusal into a confrontation.
         */
        get: operations["list_outgoing_api_v1_friends_requests_outgoing_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/friends/requests/{request_id}/accept": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Accept a friend request
         * @description Accepts a request addressed to you.
         *
         *     **Only the recipient.** The sender gets `403` — they may cancel, which
         *     is a different act leaving different history.
         *
         *     `409` when the request is no longer pending, which includes the case
         *     worth knowing about: you accepted on one device and declined on
         *     another. The second one loses, and this is what it is told.
         *
         *     **No friendship exists yet.** A64-013.2 records the resolution and
         *     stops; A64-013.3 creates the `Friendship` in this same transaction
         *     (FR-4). Accepting today is durable and correct — it simply has no friend
         *     list to appear in.
         */
        post: operations["accept_friend_request_api_v1_friends_requests__request_id__accept_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/friends/requests/{request_id}/decline": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Decline a friend request
         * @description Declines a request addressed to you.
         *
         *     **Silent to the sender** (FR-3): nothing notifies them, and the request
         *     simply leaves their outgoing list. A notified decline turns a refusal
         *     into a confrontation.
         *
         *     The row is kept rather than deleted — it is a fact with a date, and
         *     FR-5's future decline cooldown reads it.
         *
         *     **Only the recipient.** `403` for anybody else.
         */
        post: operations["decline_friend_request_api_v1_friends_requests__request_id__decline_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/friends/requests/{request_id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /**
         * Cancel a friend request you sent
         * @description Withdraws a request you sent.
         *
         *     **Only the sender.** The recipient gets `403` — they have `decline`,
         *     which reaches the same practical outcome and leaves different history.
         *
         *     `200` with a body rather than `204`, and `DELETE` rather than a `POST
         *     /cancel`, which needs stating because the two pull in opposite
         *     directions. The verb describes what the caller is doing to *their
         *     request*; the body exists because the row is not removed. Nothing here
         *     is ever deleted — database.md §1221: "a row that ended is a fact with a
         *     date; the row is history, not debris" — so the response reports the
         *     resolved request rather than an empty success a client would have to
         *     interpret.
         */
        delete: operations["cancel_friend_request_api_v1_friends_requests__request_id__delete"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/friends/count": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Count your friends
         * @description Returns your current number of friends.
         *
         *     **Your own count, always.** There is no path segment or parameter that
         *     could name another player, so this needs no ownership check — another
         *     account's friend count is not addressable here.
         *
         *     Counts **live** friendships only: one that ended is not included, in
         *     either direction.
         *
         *     A real count rather than the length of a page, so it stays correct past
         *     the first one. Still deliberately **not cached**, even though
         *     `friends:v1:` is live since A64-013.6: the count is derivable from the
         *     friend-id set that namespace already holds, and a second entry would be
         *     a second copy of one fact with its own way of going stale.
         */
        get: operations["count_friends_api_v1_friends_count_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/friends": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List your friends
         * @description Returns your friends, most recently added first.
         *
         *     **Your own list, always.** The account comes from your access token and
         *     no parameter could name a different one, so there is no ownership check
         *     here because another player's friend list is not addressable.
         *
         *     Each `player` is the full public profile — and because you are a friend,
         *     **fields they restricted to friends are visible to you**. The same
         *     profile read by a stranger hides them. That is `VisibilityLevel.FRIENDS`
         *     working end to end, and it needs nothing from a client.
         *
         *     **Keyset pagination, never offset.** Follow `page.next_cursor` until it
         *     is `null`; `page.has_more` says whether there is one. There is
         *     deliberately no total in the page — `GET /friends/count` is the endpoint
         *     for that, and computing it on every page would pay for a number most
         *     callers do not read.
         */
        get: operations["list_friends_api_v1_friends_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/friends/{player_id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Inspect one friendship
         * @description Returns your friendship with `player_id`.
         *
         *     **Only your own.** The other party comes from the path and *you* come
         *     from the access token, so the only relationship inspectable here is one
         *     you are part of — there is no arrangement of parameters that could ask
         *     about two other people. That is why there is no authorization check in
         *     this handler: the alternative it would guard against is not expressible.
         *
         *     `404` when you are not currently friends, covering "never were" and "it
         *     ended" indistinguishably. Whether two people were *ever* friends is not
         *     a question an inspection endpoint should answer.
         *
         *     `player` is the full public profile, composed exactly as
         *     `GET /profiles/{username}` composes it — and because you are a friend,
         *     **fields restricted to friends are visible to you** here and hidden from
         *     the same profile read by a stranger.
         *
         *     Registered **after** `/friends/count`, which is the same two-segment
         *     shape; Starlette matches in registration order, so the literal path
         *     leads. A contract test asserts the resolution.
         */
        get: operations["get_friendship_api_v1_friends__player_id__get"];
        put?: never;
        post?: never;
        /**
         * Remove a friend
         * @description Ends your friendship with `player_id`.
         *
         *     **Unilateral and silent** (FS-2). You do not need their agreement, and
         *     they are not told — "requiring mutual agreement to stop being friends is
         *     not a feature anyone wants", and a notified removal turns a withdrawal
         *     into a confrontation.
         *
         *     **Only your own friendships.** The other party comes from the path and
         *     *you* come from the token, so the only friendship addressable here is
         *     one you are part of. A player who is not in the pair gets `404`, not
         *     `403` — there is no friendship between them and the caller to speak of.
         *
         *     **Idempotent** (A64-013.4). Removing somebody you are not friends with
         *     returns `204` and changes nothing — it does not `404`. A client
         *     retrying after a dropped response must not be told the resource is gone
         *     when its own first attempt is what removed it.
         *
         *     It also stops the endpoint answering a question it should not. A `404`
         *     for "you are not friends" beside a `204` for "you were" is an oracle:
         *     anybody holding a player id could probe their own relationship state,
         *     and once blocking voids friendships could detect having been blocked by
         *     watching a removal turn into a `404`. One answer for both closes that.
         *
         *     Contrast `GET /friends/{player_id}`, which *does* `404`: a read of a
         *     resource that does not exist has no other honest answer, and a `GET` has
         *     no idempotency contract to honour.
         *
         *     `204` with no body, unlike `DELETE /friends/requests/{id}`, and the
         *     difference is real rather than inconsistent: cancelling a request
         *     returns the request in its resolved state because a client shows it in a
         *     list; a removed friendship simply leaves the list, and there is nothing
         *     left to render. The row is still kept — database.md §1221: a friendship
         *     that ended is a fact with a date, and it is what lets the two be friends
         *     again later.
         */
        delete: operations["remove_friend_api_v1_friends__player_id__delete"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/blocks": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List the players you have blocked
         * @description Returns the players **you have blocked**, most recently first.
         *
         *     **Only blocks you placed.** Blocks placed *on* you never appear here or
         *     anywhere else — that invisibility is the whole reason a block is worth
         *     placing (BL-1), and it is a property of this query rather than a filter
         *     somebody remembers to apply.
         *
         *     **Your own list, always.** The account comes from your access token and
         *     no parameter could name a different one, so there is no ownership check
         *     here because another player's block list is not addressable.
         *
         *     **Keyset pagination, never offset.** Follow `page.next_cursor` until it
         *     is `null`.
         */
        get: operations["list_blocked_players_api_v1_blocks_get"];
        put?: never;
        /**
         * Block a player
         * @description Blocks `player_id` on your behalf, and applies the consequences at
         *     once.
         *
         *     **The blocked player is never told** (BL-1). Nothing notifies them, no
         *     response of theirs changes shape, and there is no endpoint anywhere that
         *     reports being blocked. A visible block is an invitation to retaliate
         *     from a second account.
         *
         *     ## What happens in the same transaction
         *
         *     | Effect | Rule |
         *     | --- | --- |
         *     | any friendship between you ends, reason `blocked` | FS-3 |
         *     | any pending friend request, either direction, is voided | FR-2 |
         *     | they disappear from your search results, and you from theirs | BL-2 |
         *     | neither of you sees the other's presence or friends-only fields | BL-2 |
         *
         *     All of it commits together. A block that suppressed future contact while
         *     leaving the friendship live would be the block silently not working.
         *
         *     **Not idempotent** — a second block is a `409`. Blocking runs a cascade,
         *     and reporting success for a repeat would claim a cascade ran that did
         *     not. Contrast `DELETE /blocks/{player_id}`, which has no cascade and is
         *     idempotent.
         *
         *     **Nothing is restored on unblock.** A friendship this ended stays ended
         *     and you must send a fresh request. BL-3: blocks do not rewrite history,
         *     in either direction.
         */
        post: operations["block_player_api_v1_blocks_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/blocks/{player_id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /**
         * Unblock a player
         * @description Lifts your block on `player_id`.
         *
         *     **Only your own blocks.** The block comes from your token plus the path,
         *     so a block you did not place is not addressable — including one placed
         *     on you, which you cannot see and cannot lift.
         *
         *     **Idempotent.** Unblocking somebody you have not blocked returns `204`
         *     and changes nothing. A `DELETE` is idempotent by HTTP semantics, and one
         *     answer for both cases keeps this from reporting your own block-list
         *     state back to you differently depending on it.
         *
         *     **Restores nothing.** A friendship the block ended stays ended and a
         *     friend request it voided stays voided — you must start again. BL-3 cuts
         *     both ways: the block did not erase the friendship, and lifting it does
         *     not resurrect one.
         */
        delete: operations["unblock_player_api_v1_blocks__player_id__delete"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/matchmaking/queue": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Join a matchmaking queue
         * @description Enters your account into a matchmaking pool.
         *
         *     `201`, because a ticket is a new resource with an identifier the
         *     response carries.
         *
         *     **Your ticket may become a match.** Since A64-015.4 a background scan
         *     pairs waiting players and creates a match for them, so a ticket ends in
         *     one of four ways: you leave, it expires, or it is consumed by a pairing
         *     — at which point `GET /matchmaking/queue/me` answers `404` and
         *     `GET /matchmaking/matches/pending` has your offer.
         *
         *     Since A64-015.5 that offer is **pushed** to a connected client rather
         *     than waited for; the polling endpoint is the reconnect fallback.
         *
         *     ## Two `409`s, and they mean different things
         *
         *     `conflict` — you already hold a live ticket. Leave it, or answer the
         *     match you have.
         *
         *     `queue_cooldown_active` — you declined a match recently. Nothing to
         *     undo; `Retry-After` says how many seconds until you may queue again.
         *     Declining is the only thing that earns this: letting a match's window
         *     close in silence does not.
         *
         *     ## One ticket, across every pool
         *
         *     `409` if you already hold one, **including in a different pool**.
         *     Multi-queueing means being paired into two simultaneous matches, one of
         *     which must then be abandoned — which looks, to the opponent whose game
         *     vanished, exactly like a stolen win.
         *
         *     ## What you are refused for, and what you are not
         *
         *     | Outcome | Rule |
         *     | --- | --- |
         *     | `409` | You already hold a live ticket (QT-1) |
         *     | `422` | The platform has recorded you as signed out |
         *
         *     The `422` fires only on a *recorded* sign-out. Presence that is simply
         *     unknown — you have not been observed recently, or the presence store is
         *     unavailable — is permitted, because those cases are indistinguishable
         *     by design and refusing on them would make a cache blip an outage of
         *     matchmaking.
         *
         *     ## The rating is not yours to send
         *
         *     `rating_snapshot` in the response is the rating your ticket was entered
         *     with, recorded by the platform and fixed for the ticket's life. A rating
         *     that changes while you wait does not move your place; re-queueing is
         *     what picks up a new one. There is deliberately no way to supply it.
         *
         *     Every rating is provisional today — no game has been played on this
         *     platform and no rating system exists yet — so every ticket records the
         *     same starting value.
         */
        post: operations["join_queue_api_v1_matchmaking_queue_post"];
        /**
         * Leave the matchmaking queue
         * @description Withdraws your queue ticket.
         *
         *     **Your own, always.** There is no path segment or body field naming a
         *     ticket, so another player's is not addressable — which is why this
         *     handler has no ownership check.
         *
         *     **Idempotent.** Leaving a queue you are not in returns `204` and changes
         *     nothing; it does not `404`. A client retrying after a dropped response
         *     must not be told the resource is gone when its own first attempt is what
         *     removed it, and one answer for both cases keeps this from reporting your
         *     queue state back through a status code.
         *
         *     Contrast `GET /matchmaking/queue/me`, which *does* `404`: a read of a
         *     resource that does not exist has no other honest answer, and a `GET` has
         *     no idempotency contract to honour.
         *
         *     `204` with no body. Unlike a cancelled friend request — which is
         *     returned in its resolved state because a client shows it in a list — a
         *     withdrawn ticket simply stops existing as far as any client surface is
         *     concerned. The row is kept and marked `cancelled`, because how long
         *     somebody was prepared to wait is a fact worth having.
         */
        delete: operations["leave_queue_api_v1_matchmaking_queue_delete"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/matchmaking/queue/me": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Read your queue ticket
         * @description Returns your live queue ticket.
         *
         *     **Your own, always** — the account comes from your access token and no
         *     parameter could name a different one. There is deliberately no endpoint
         *     that reads somebody else's ticket: who is queueing right now is exactly
         *     the information that would let a player wait for a favourable pool.
         *
         *     `404` when you have no live ticket, covering "never joined", "left",
         *     "expired" and "matched" indistinguishably. Which of the four applies is
         *     not something this endpoint should answer, and a client's next move is
         *     the same for all of them.
         *
         *     **A ticket past its deadline reads as absent** even in the moment before
         *     a worker records it as expired. `expires_at` is the rule; the sweep is
         *     bookkeeping, and a player must never be told they are queued because a
         *     background job is a few seconds behind — nor be blocked from re-queueing
         *     by one.
         *
         *     **Not rate limited**, unlike the two writes. This is the endpoint a
         *     client polls while waiting, and throttling it would make a working queue
         *     look broken in exactly the situation it is working.
         *
         *     `waiting` counts everyone in your pool, including you. It is a reading
         *     rather than a position — the queue is ordered by entry time for pairing,
         *     but nobody is "third in line", because the pool is scanned by rating
         *     rather than drained in order.
         */
        get: operations["read_my_ticket_api_v1_matchmaking_queue_me_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/matchmaking/matches/pending": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Read the match awaiting your answer
         * @description Returns the match you have been paired into and not yet answered.
         *
         *     **Yours, always** — the account comes from your access token and no
         *     parameter could name a different one. There is deliberately no endpoint
         *     that reads somebody else's pending match: who is being paired with whom
         *     right now is exactly what would let a player wait for a favourable draw.
         *
         *     **At most one, by construction.** You hold one live queue ticket, a
         *     ticket produces at most one match, and a pending match holds you until
         *     it settles — so this is singular without needing a rule of its own.
         *
         *     `404` when there is none, covering "never paired", "already answered"
         *     and "the offer expired" indistinguishably. Which applies is not
         *     something this endpoint should answer, and your next move is the same
         *     for all three: rejoin the queue.
         *
         *     ## Answer inside `acceptance_deadline`
         *
         *     It is an instant rather than a countdown, so a slow response cannot make
         *     your timer wrong. An answer that arrives after it is refused, and the
         *     match is expired for both of you shortly afterwards by a background job
         *     — the deadline is the rule, and the job is only the bookkeeping.
         *
         *     ## This is the fallback, not the primary delivery
         *
         *     Since A64-015.5 a pending match is **pushed**: `game.match_created`
         *     reaches a realtime consumer through the transactional outbox, which
         *     re-reads the match, re-checks the block graph and hands it to the
         *     gateway. A connected client learns about its match without asking.
         *
         *     This endpoint remains, and is deliberately not deprecated. It is what a
         *     client uses to **recover**:
         *
         *     | Situation | Why polling is the answer |
         *     | --- | --- |
         *     | Reconnect | The push happened while the socket was down |
         *     | Cold start | A client that has just opened does not know what it missed |
         *     | Push disabled | `MATCHMAKING_REALTIME_DELIVERY_ENABLED=false` is a real deployment |
         *     | Doubt | The authoritative state is the database, and this reads it |
         *
         *     A client that only polls still works correctly; it simply learns later.
         *     A client that only listens is correct until the first dropped
         *     connection, which is why this must not become optional.
         *
         *     **Not rate limited**, unlike the two answers. This is the endpoint a
         *     client polls while deciding *and* the one it calls on every reconnect,
         *     and throttling it would make a working handshake look broken in exactly
         *     the situation it is working.
         */
        get: operations["read_pending_match_api_v1_matchmaking_matches_pending_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/matchmaking/matches/{match_id}/accept": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Accept a match you have been offered
         * @description Says yes to a match you were paired into.
         *
         *     **For yourself only.** There is no side or player field anywhere in the
         *     request — which seat you hold is derived from your access token — so
         *     accepting on your opponent's behalf is not something this API can
         *     express.
         *
         *     The match becomes `active` when *both* of you have accepted, and not
         *     before. Until then the response reports `status: pending_acceptance`
         *     with `you_accepted: true`, which is the honest state: you have agreed
         *     and are waiting.
         *
         *     **Idempotent.** Accepting twice returns the same match rather than a
         *     `409` — a client retrying after a dropped response asked for something
         *     that is already true, and telling it otherwise would make a network
         *     blip look like a lost game.
         *
         *     `404` for a match that does not exist **and** for one that is not
         *     yours. `409` once the handshake is over — your opponent declined, or
         *     the window closed — and `409` for an answer that arrives after
         *     `acceptance_deadline`, whether or not a background job has recorded the
         *     expiry yet.
         */
        post: operations["accept_match_api_v1_matchmaking_matches__match_id__accept_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/matchmaking/matches/{match_id}/decline": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Decline a match you have been offered
         * @description Says no to a match you were paired into.
         *
         *     **One decline ends it**, whatever your opponent did. The match is
         *     `cancelled` and no game is created.
         *
         *     ## Neither of you goes back in the queue
         *
         *     Your queue ticket was consumed the moment the match was created, and
         *     declining does not restore it — for you or for an opponent who had
         *     already accepted. Both of you must join the queue again.
         *
         *     That is the platform's current behaviour and it is a **stated choice
         *     rather than a finished policy**: `specs/matchmaking.md` lists what a
         *     declined acceptance should do to both tickets as an open specification
         *     item, and re-queueing somebody automatically raises four product
         *     questions nobody has answered — whose place in line survives, whether
         *     the decliner waits, what rating snapshot the new ticket carries, and
         *     what happens if they have already re-queued by hand.
         *
         *     `200` rather than `204`: the response carries the settled match, so a
         *     client can render "cancelled" without a second read.
         *
         *     A second decline is a `409`, not a repeat — by then the match is
         *     already cancelled and there is nothing left to refuse. A client that
         *     needs to be safe against its own retry reads the match instead.
         */
        post: operations["decline_match_api_v1_matchmaking_matches__match_id__decline_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/players/{player_id}/matches": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * A player's finished matches
         * @description Finished matches, newest first, keyset-paginated.
         *
         *     **The viewer is the authenticated user**, never a value from the
         *     request: `player_id` says *whose* history to read and `user.id` says who
         *     is asking, and a client cannot swap them. Rated matches are public;
         *     casual ones appear only when the viewer played in them.
         *
         *     Never replays anything — an entry is stored facts, which is why a match
         *     whose replay is refused still appears here.
         */
        get: operations["player_match_history_api_v1_players__player_id__matches_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/matches/{match_id}/replay": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Replay a finished match
         * @description One finished match, played back ply by ply.
         *
         *     A match that does not exist and a casual match the viewer did not play
         *     produce the **same** `404` — see this module's docstring.
         *
         *     A match played under an unsupported engine version raises
         *     `UnsupportedEngineVersion`, which the platform's handler maps to a
         *     stable code. No replay is attempted: A64-014.8 refuses rather than
         *     approximating, because a reconstruction under fixed rules could end
         *     differently from the game that was rated and displayed.
         */
        get: operations["match_replay_api_v1_matches__match_id__replay_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/ratings/me": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Your own ratings
         * @description Every speed class, for the authenticated player.
         *
         *     **One query for all of them** — `ratings_across` exists precisely so a
         *     page showing several speed classes side by side is not one query per
         *     class (the N+1 `rating.public` records).
         *
         *     A class the player has never played is present and marked provisional
         *     with zero games, because `rating` answers an absent row with
         *     `RatingSnapshot.unrated()` rather than with nothing. Omitting them would
         *     push "has this player played blitz?" onto every client.
         */
        get: operations["my_ratings_api_v1_ratings_me_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/players/{player_id}/ratings": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * A player's ratings
         * @description The same summary, for anybody.
         *
         *     No `404` for an unknown id, deliberately: `rating` answers every player
         *     with a snapshot, so "this account does not exist" and "this account has
         *     never played" are indistinguishable here — and making them
         *     distinguishable would turn this endpoint into an account-existence
         *     oracle. Whether a player exists is `users`' question.
         */
        get: operations["player_ratings_api_v1_players__player_id__ratings_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/leaderboard": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * One key's ladder
         * @description Standings for one key, best first, keyset-paginated.
         *
         *     **Never `OFFSET`.** A ladder moves while it is read, so an offset page
         *     can show a player twice or skip them entirely as ratings shift between
         *     requests; a cursor names the *row* it resumed after, which keeps its
         *     identity when the ratings around it move.
         *
         *     Ordered `rating DESC, deviation ASC, player_id ASC` — total, so the
         *     pagination is stable. Provisional players are ranked and shown, never
         *     hidden (SPEC-RATING §6).
         */
        get: operations["leaderboard_api_v1_leaderboard_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/leaderboard/around/{player_id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Where a player stands
         * @description One player's rank and the rows either side — the "where am I?" read.
         *
         *     A client cannot answer this by paging: finding yourself on a ladder of
         *     any size is a linear scan, and the rank is a property of the whole
         *     relation rather than of a page.
         *
         *     A player with **no rating in this key** is a `404`: they are not on this
         *     ladder, and there is no position to return. That is different from
         *     `/players/{id}/ratings`, which answers every id — a rating exists for
         *     everybody, a *ranking* only for those who have one stored.
         */
        get: operations["leaderboard_around_api_v1_leaderboard_around__player_id__get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/tournaments": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * The tournament lobby
         * @description Every public tournament, newest first — A64-020.0B.
         *
         *     **Keyset**, ordered `created_at DESC, id DESC`. Total, because two
         *     tournaments created in the same millisecond would otherwise page
         *     unstably; never `OFFSET`, whose cost grows with the page number and
         *     whose window shifts the moment a tournament is created mid-walk.
         *
         *     **Every status by default**, completed and cancelled among them. A lobby
         *     that hid finished tournaments would answer "what happened here?" with
         *     silence, and `status` is what narrows the view to what a player can
         *     still enter.
         *
         *     Public in the same sense as everything else here (§7): visible to every
         *     authenticated player, with no viewer narrower than another. Private
         *     tournaments do not exist in v0.x, so there is no visibility predicate to
         *     get wrong.
         *
         *     The five filters are a **closed set** and each is an enum or a boolean
         *     the tournament already stores, so an unknown value is a `422` from
         *     FastAPI's own validation rather than an empty page that looks like a
         *     combination nobody runs. There is no creator filter, no free text and no
         *     caller-chosen ordering: each would be either a product decision nobody
         *     has made or a scan whose cost this endpoint could not state.
         */
        get: operations["tournament_lobby_api_v1_tournaments_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/tournaments/{tournament_id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * One tournament
         * @description A tournament's public detail — §9.
         *
         *     Everything a lobby or a detail page renders: the configuration, how full
         *     it is, which round is being played, and the three lifecycle instants.
         */
        get: operations["tournament_detail_api_v1_tournaments__tournament_id__get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/tournaments/{tournament_id}/bracket": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * A tournament's bracket
         * @description Every round, node and attempt — §10.
         *
         *     Read whole rather than paged: a field is at most 128 (T-2), so a bracket
         *     is at most 127 nodes and rendering one needs all of them.
         *
         *     A tournament whose bracket has not been materialised answers with an
         *     empty round list rather than a `404` — the tournament exists, and "no
         *     bracket yet" is a state a client renders rather than an error.
         */
        get: operations["tournament_bracket_api_v1_tournaments__tournament_id__bracket_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/tournaments/{tournament_id}/standings": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * A tournament's final standings
         * @description The immutable final placement — §11.
         *
         *     Ordered by rank, then seed, then player id. **Empty while the tournament
         *     is being played**: standings are materialised once, when it completes
         *     (§6f), and nothing here derives a partial one — a placement that changed
         *     between two reads would not be a result.
         */
        get: operations["tournament_standings_api_v1_tournaments__tournament_id__standings_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/tournaments/{tournament_id}/registrations": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Enter a tournament
         * @description Enters **the authenticated player** — A64-019.8 §1.
         *
         *     There is no request body. The player is `user.id` and nothing else:
         *     the route has no parameter that could name somebody else, so entering
         *     another person is not refused, it is unrepresentable.
         *
         *     Every refusal is the service's, raised under the row lock and mapped to
         *     a stable code by its own type — `registration_not_open`,
         *     `registration_deadline_passed`, `tournament_full`, `already_registered`.
         *     Nothing is re-checked here: a route that repeated the capacity test
         *     would be a second copy that could disagree, and it would be outside the
         *     lock that makes the first one correct.
         *
         *     **Idempotency is a conflict, not a repeat.** A second entry answers
         *     `409 already_registered` rather than `201`, because the unique key
         *     refused an insert and pretending otherwise would report a registration
         *     this call did not make. A client reconciling a dropped response reads
         *     the code and treats it as success.
         */
        post: operations["enter_tournament_api_v1_tournaments__tournament_id__registrations_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/tournaments/{tournament_id}/registrations/me": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /**
         * Withdraw from a tournament
         * @description Withdraws **the authenticated player's own** entry — §1.
         *
         *     `/me` rather than a player id in the path, so there is no shape in
         *     which this endpoint acts on somebody else — the same reason the entry
         *     route has no body.
         *
         *     Allowed **only before registration closes**. After that the field is
         *     fixed and the bracket is built from exactly those players, so a
         *     withdrawal would leave a seat nothing fills; it is refused rather than
         *     converted to a forfeit, because a forfeit is a *match* outcome and
         *     there is no match yet.
         *
         *     The row survives with `status = withdrawn` — §7's append-oriented
         *     record. A repeated withdrawal answers `404 registration_not_found`,
         *     which is the same answer as never having entered and is what makes the
         *     call safe to send twice: the resource `/registrations/me` is gone
         *     either way, and `withdrawn_at` cannot move.
         */
        delete: operations["withdraw_from_tournament_api_v1_tournaments__tournament_id__registrations_me_delete"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/players/{player_id}/tournaments": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * A player's tournaments
         * @description Completed and active participation, newest first — §12.
         *
         *     **Keyset**, never `OFFSET`: a history is unbounded, and an offset scan
         *     costs more with every page and shifts when a row is inserted mid-scan.
         *
         *     Public, like everything else here: `player_id` says whose history to
         *     read and there is no narrowing by viewer, because a tournament entry is
         *     a public competitive record. `final_rank` and `final_status` are `null`
         *     while a tournament is running, which is what tells a client to render
         *     "in progress" rather than a placing.
         */
        get: operations["player_tournaments_api_v1_players__player_id__tournaments_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
}
export type webhooks = Record<string, never>;
export interface components {
    schemas: {
        /**
         * AnimationSpeed
         * @description How fast a piece slides to its square.
         *
         *     `INSTANT` is a real accessibility setting rather than a fourth speed:
         *     motion is a migraine and vestibular trigger, and a player who needs it
         *     off needs it off rather than fast. Spelled as a member of this enum
         *     instead of a separate `animations_enabled` boolean so a client renders
         *     one control, and so "off" cannot disagree with a speed set beside it.
         * @enum {string}
         */
        AnimationSpeed: "instant" | "fast" | "normal" | "slow";
        /** ApiResponse[AvatarResponse] */
        ApiResponse_AvatarResponse_: {
            data: components["schemas"]["AvatarResponse"];
            meta: components["schemas"]["ResponseMeta"];
        };
        /** ApiResponse[BlockedPlayerResponse] */
        ApiResponse_BlockedPlayerResponse_: {
            data: components["schemas"]["BlockedPlayerResponse"];
            meta: components["schemas"]["ResponseMeta"];
        };
        /** ApiResponse[BracketResponse] */
        ApiResponse_BracketResponse_: {
            data: components["schemas"]["BracketResponse"];
            meta: components["schemas"]["ResponseMeta"];
        };
        /** ApiResponse[CursorPage[BlockedPlayerResponse]] */
        ApiResponse_CursorPage_BlockedPlayerResponse__: {
            data: components["schemas"]["CursorPage_BlockedPlayerResponse_"];
            meta: components["schemas"]["ResponseMeta"];
        };
        /** ApiResponse[CursorPage[FriendRequestResponse]] */
        ApiResponse_CursorPage_FriendRequestResponse__: {
            data: components["schemas"]["CursorPage_FriendRequestResponse_"];
            meta: components["schemas"]["ResponseMeta"];
        };
        /** ApiResponse[CursorPage[FriendResponse]] */
        ApiResponse_CursorPage_FriendResponse__: {
            data: components["schemas"]["CursorPage_FriendResponse_"];
            meta: components["schemas"]["ResponseMeta"];
        };
        /** ApiResponse[CursorPage[ProfileResponse]] */
        ApiResponse_CursorPage_ProfileResponse__: {
            data: components["schemas"]["CursorPage_ProfileResponse_"];
            meta: components["schemas"]["ResponseMeta"];
        };
        /** ApiResponse[CursorPage[PublicUserResponse]] */
        ApiResponse_CursorPage_PublicUserResponse__: {
            data: components["schemas"]["CursorPage_PublicUserResponse_"];
            meta: components["schemas"]["ResponseMeta"];
        };
        /** ApiResponse[FriendCountResponse] */
        ApiResponse_FriendCountResponse_: {
            data: components["schemas"]["FriendCountResponse"];
            meta: components["schemas"]["ResponseMeta"];
        };
        /** ApiResponse[FriendRequestResponse] */
        ApiResponse_FriendRequestResponse_: {
            data: components["schemas"]["FriendRequestResponse"];
            meta: components["schemas"]["ResponseMeta"];
        };
        /** ApiResponse[FriendshipDetailsResponse] */
        ApiResponse_FriendshipDetailsResponse_: {
            data: components["schemas"]["FriendshipDetailsResponse"];
            meta: components["schemas"]["ResponseMeta"];
        };
        /** ApiResponse[LeaderboardNeighbourhoodResponse] */
        ApiResponse_LeaderboardNeighbourhoodResponse_: {
            data: components["schemas"]["LeaderboardNeighbourhoodResponse"];
            meta: components["schemas"]["ResponseMeta"];
        };
        /** ApiResponse[LeaderboardResponse] */
        ApiResponse_LeaderboardResponse_: {
            data: components["schemas"]["LeaderboardResponse"];
            meta: components["schemas"]["ResponseMeta"];
        };
        /** ApiResponse[LivenessResponse] */
        ApiResponse_LivenessResponse_: {
            data: components["schemas"]["LivenessResponse"];
            meta: components["schemas"]["ResponseMeta"];
        };
        /** ApiResponse[MatchHistoryResponse] */
        ApiResponse_MatchHistoryResponse_: {
            data: components["schemas"]["MatchHistoryResponse"];
            meta: components["schemas"]["ResponseMeta"];
        };
        /** ApiResponse[MatchReplayResponse] */
        ApiResponse_MatchReplayResponse_: {
            data: components["schemas"]["MatchReplayResponse"];
            meta: components["schemas"]["ResponseMeta"];
        };
        /** ApiResponse[MyProfileResponse] */
        ApiResponse_MyProfileResponse_: {
            data: components["schemas"]["MyProfileResponse"];
            meta: components["schemas"]["ResponseMeta"];
        };
        /** ApiResponse[PendingMatchResponse] */
        ApiResponse_PendingMatchResponse_: {
            data: components["schemas"]["PendingMatchResponse"];
            meta: components["schemas"]["ResponseMeta"];
        };
        /** ApiResponse[PlayerRatingsResponse] */
        ApiResponse_PlayerRatingsResponse_: {
            data: components["schemas"]["PlayerRatingsResponse"];
            meta: components["schemas"]["ResponseMeta"];
        };
        /** ApiResponse[PlayerTournamentsResponse] */
        ApiResponse_PlayerTournamentsResponse_: {
            data: components["schemas"]["PlayerTournamentsResponse"];
            meta: components["schemas"]["ResponseMeta"];
        };
        /** ApiResponse[PreferencesResponse] */
        ApiResponse_PreferencesResponse_: {
            data: components["schemas"]["PreferencesResponse"];
            meta: components["schemas"]["ResponseMeta"];
        };
        /** ApiResponse[PrivacySettingsResponse] */
        ApiResponse_PrivacySettingsResponse_: {
            data: components["schemas"]["PrivacySettingsResponse"];
            meta: components["schemas"]["ResponseMeta"];
        };
        /** ApiResponse[ProfileResponse] */
        ApiResponse_ProfileResponse_: {
            data: components["schemas"]["ProfileResponse"];
            meta: components["schemas"]["ResponseMeta"];
        };
        /** ApiResponse[PublicUserResponse] */
        ApiResponse_PublicUserResponse_: {
            data: components["schemas"]["PublicUserResponse"];
            meta: components["schemas"]["ResponseMeta"];
        };
        /** ApiResponse[QueueTicketResponse] */
        ApiResponse_QueueTicketResponse_: {
            data: components["schemas"]["QueueTicketResponse"];
            meta: components["schemas"]["ResponseMeta"];
        };
        /** ApiResponse[ReadinessResponse] */
        ApiResponse_ReadinessResponse_: {
            data: components["schemas"]["ReadinessResponse"];
            meta: components["schemas"]["ResponseMeta"];
        };
        /** ApiResponse[RegistrationResponse] */
        ApiResponse_RegistrationResponse_: {
            data: components["schemas"]["RegistrationResponse"];
            meta: components["schemas"]["ResponseMeta"];
        };
        /** ApiResponse[StandingsResponse] */
        ApiResponse_StandingsResponse_: {
            data: components["schemas"]["StandingsResponse"];
            meta: components["schemas"]["ResponseMeta"];
        };
        /** ApiResponse[TokenPair] */
        ApiResponse_TokenPair_: {
            data: components["schemas"]["TokenPair"];
            meta: components["schemas"]["ResponseMeta"];
        };
        /** ApiResponse[TournamentListResponse] */
        ApiResponse_TournamentListResponse_: {
            data: components["schemas"]["TournamentListResponse"];
            meta: components["schemas"]["ResponseMeta"];
        };
        /** ApiResponse[TournamentResponse] */
        ApiResponse_TournamentResponse_: {
            data: components["schemas"]["TournamentResponse"];
            meta: components["schemas"]["ResponseMeta"];
        };
        /** ApiResponse[UserRead] */
        ApiResponse_UserRead_: {
            data: components["schemas"]["UserRead"];
            meta: components["schemas"]["ResponseMeta"];
        };
        /** ApiResponse[VerificationAccepted] */
        ApiResponse_VerificationAccepted_: {
            data: components["schemas"]["VerificationAccepted"];
            meta: components["schemas"]["ResponseMeta"];
        };
        /** ApiResponse[WebSocketTicketRead] */
        ApiResponse_WebSocketTicketRead_: {
            data: components["schemas"]["WebSocketTicketRead"];
            meta: components["schemas"]["ResponseMeta"];
        };
        /**
         * AttemptResponse
         * @description One `game` match played for a node — §10.
         *
         *     `match_id` is published so a reader can follow a node to the game that
         *     decided it. The moves are not: a replay is `game`'s to serve, on its own
         *     endpoint and under its own visibility rule.
         */
        AttemptResponse: {
            /** Attempt Number */
            attempt_number: number;
            /**
             * Match Id
             * Format: uuid
             */
            match_id: string;
            /**
             * Light Player Id
             * Format: uuid
             */
            light_player_id: string;
            /**
             * Dark Player Id
             * Format: uuid
             */
            dark_player_id: string;
            /** Status */
            status: string;
            /** Outcome */
            outcome?: string | null;
            /** Winner Id */
            winner_id?: string | null;
        };
        /**
         * AvatarDimensions
         * @description Both renditions' dimensions, as stored.
         *
         *     Neither is necessarily square: a non-square upload is fitted *inside*
         *     the bounding box rather than cropped, because choosing which part of
         *     somebody's picture to discard is a product decision nobody has made. A
         *     3:2 photo becomes 512x341, not 512x512.
         */
        AvatarDimensions: {
            original: components["schemas"]["ImageDimensions"];
            thumbnail: components["schemas"]["ImageDimensions"];
        };
        /**
         * AvatarReference
         * @description Where a player's avatar is stored, and how stale a cached copy is.
         *
         *     Published rather than kept private because three modules need it and
         *     none of them may reach into `users`' table: `avatars` writes it,
         *     `profiles` renders it, and both do so through ports.
         *
         *     **Carries no URL, deliberately.** `object_key` is the object's address
         *     in whatever store the deployment uses, and turning it into something a
         *     browser can fetch is `StorageProvider.get_public_url`'s job. A DTO with
         *     a URL on it would mean the module that built it had to know the CDN
         *     hostname, which is exactly the coupling A64-012.2's "public URL must be
         *     generated by StorageProvider" forbids.
         */
        AvatarReference: {
            /** Object Key */
            object_key: string | null;
            /** Version */
            version: number;
            /** Uploaded At */
            uploaded_at: string | null;
        };
        /**
         * AvatarResponse
         * @description An account's avatar as it now stands.
         *
         *     Returned by all three endpoints — see this module's docstring.
         * @example {
         *       "avatar_url": "http://localhost:8000/media/avatars/019fb9ea-0a0c-7cec-9c5f-402727c31a96/DkP2n1Qe.webp?v=2",
         *       "avatar_version": 2,
         *       "dimensions": {
         *         "original": {
         *           "height": 341,
         *           "width": 512
         *         },
         *         "thumbnail": {
         *           "height": 85,
         *           "width": 128
         *         }
         *       },
         *       "thumbnail_url": "http://localhost:8000/media/avatars/019fb9ea-0a0c-7cec-9c5f-402727c31a96/DkP2n1Qe_thumb.webp?v=2",
         *       "uploaded_at": "2026-08-01T12:00:00Z"
         *     }
         */
        AvatarResponse: {
            /**
             * Avatar Url
             * @description URL of the full-size rendition, at most 512px on its longest edge. `null` when the account has no avatar. Carries a `?v=` cache-buster that changes on every upload and every delete.
             * @example http://localhost:8000/media/avatars/019fb9ea-.../DkP2n1Qe.webp?v=2
             */
            avatar_url?: string | null;
            /**
             * Thumbnail Url
             * @description URL of the 128px rendition, for listings and match cards. `null` when the account has no avatar.
             * @example http://localhost:8000/media/avatars/019fb9ea-.../DkP2n1Qe_thumb.webp?v=2
             */
            thumbnail_url?: string | null;
            /**
             * Avatar Version
             * @description Increments on every upload **and** every delete. Present even when there is no avatar, because a client that cached the previous one needs the change signal precisely then. Use it to bust caches, not to count uploads — it starts at 1 before the first upload.
             * @example 2
             */
            avatar_version: number;
            /**
             * Uploaded At
             * @description When the current avatar was stored, UTC. `null` when there is none.
             * @example 2026-08-01T12:00:00Z
             */
            uploaded_at?: string | null;
            /** @description The stored pixel sizes. Returned **only by `POST`**, where they are known from the encode that just happened; `GET` and `DELETE` omit them rather than decoding the stored file to report them. */
            dimensions?: components["schemas"]["AvatarDimensions"] | null;
        };
        /**
         * BlockPlayerRequest
         * @description The `POST /blocks` body.
         *
         *     One field, and it is a `player_id` rather than a username — the same
         *     choice `SendFriendRequestRequest` makes, for the same reason: a client
         *     blocking somebody is looking at a search result, a profile or a match
         *     card, all of which carry `id`, and DM-06 makes it the only reference
         *     that crosses a context boundary.
         * @example {
         *       "player_id": "019fb9ea-0a0c-7cec-9c5f-402727c31a96"
         *     }
         */
        BlockPlayerRequest: {
            /**
             * Player Id
             * Format: uuid
             * @description The player to block. Blocking ends any friendship between you, voids any pending friend request in either direction, and removes them from your search results — all at once. They are **not** notified.
             * @example 019fb9ea-0a0c-7cec-9c5f-402727c31a96
             */
            player_id: string;
        };
        /**
         * BlockedPlayerResponse
         * @description One blocked player, with the date the block was placed.
         * @example {
         *       "blocked_at": "2026-08-01T12:00:00Z",
         *       "player": {
         *         "country": "GB",
         *         "display_name": "Player One",
         *         "id": "019fb9ea-0a0c-7cec-9c5f-402727c31a96",
         *         "joined_at": "2026-07-01T09:30:00Z",
         *         "language": "en",
         *         "ratings": {
         *           "blitz": {
         *             "games_played": 0,
         *             "is_provisional": true,
         *             "rating": 1500
         *           },
         *           "classic": {
         *             "games_played": 0,
         *             "is_provisional": true,
         *             "rating": 1500
         *           },
         *           "rapid": {
         *             "games_played": 0,
         *             "is_provisional": true,
         *             "rating": 1500
         *           }
         *         },
         *         "statistics": {
         *           "best_win_streak": 0,
         *           "current_rating": 1500,
         *           "current_streak": 0,
         *           "draws": 0,
         *           "games_played": 0,
         *           "highest_rating": 1500,
         *           "losses": 0,
         *           "win_rate": 0,
         *           "wins": 0
         *         },
         *         "username": "player_one"
         *       }
         *     }
         */
        BlockedPlayerResponse: {
            /** @description The player you blocked — the same public profile shape `GET /profiles/{username}` returns, so you can recognise who you are about to unblock. */
            player: components["schemas"]["ProfileResponse"];
            /**
             * Blocked At
             * Format: date-time
             * @description When you placed the block, UTC.
             * @example 2026-08-01T12:00:00Z
             */
            blocked_at: string;
        };
        /**
         * BoardTheme
         * @description The board's colour scheme. Rendered entirely by the client.
         * @enum {string}
         */
        BoardTheme: "classic" | "wood" | "marble" | "midnight";
        /** Body_upload_avatar_api_v1_profile_avatar_post */
        Body_upload_avatar_api_v1_profile_avatar_post: {
            /**
             * File
             * @description The image. Accepted: image/jpeg, image/png, image/webp. At most 5 MB.
             */
            file: string;
        };
        /**
         * BracketNodeResponse
         * @description One node of the published bracket — §10.
         */
        BracketNodeResponse: {
            /**
             * Pairing Id
             * Format: uuid
             */
            pairing_id: string;
            /** Round Number */
            round_number: number;
            /** Slot */
            slot: number;
            /** Light Player Id */
            light_player_id?: string | null;
            /** Dark Player Id */
            dark_player_id?: string | null;
            /** Light Seed */
            light_seed?: number | null;
            /** Dark Seed */
            dark_seed?: number | null;
            /** Winner Id */
            winner_id?: string | null;
            /**
             * Advancement Reason
             * @description played, bye or adjudication.
             */
            advancement_reason?: string | null;
            /** Is Bye */
            is_bye: boolean;
            /** Attempts */
            attempts: components["schemas"]["AttemptResponse"][];
        };
        /** BracketResponse */
        BracketResponse: {
            /**
             * Tournament Id
             * Format: uuid
             */
            tournament_id: string;
            /** Rounds */
            rounds: components["schemas"]["RoundResponse"][];
        };
        /**
         * CursorPageInfo
         * @description Metadata for a keyset-paginated result. Deliberately no `total`:
         *     RP-03's whole point is avoiding a count that gets slower the deeper a
         *     page goes, so a keyset result never claims to know how many rows
         *     exist in total.
         */
        CursorPageInfo: {
            /** Next Cursor */
            next_cursor: string | null;
            /** Has More */
            has_more: boolean;
        };
        /** CursorPage[BlockedPlayerResponse] */
        CursorPage_BlockedPlayerResponse_: {
            /** Items */
            items: components["schemas"]["BlockedPlayerResponse"][];
            page: components["schemas"]["CursorPageInfo"];
        };
        /** CursorPage[FriendRequestResponse] */
        CursorPage_FriendRequestResponse_: {
            /** Items */
            items: components["schemas"]["FriendRequestResponse"][];
            page: components["schemas"]["CursorPageInfo"];
        };
        /** CursorPage[FriendResponse] */
        CursorPage_FriendResponse_: {
            /** Items */
            items: components["schemas"]["FriendResponse"][];
            page: components["schemas"]["CursorPageInfo"];
        };
        /** CursorPage[ProfileResponse] */
        CursorPage_ProfileResponse_: {
            /** Items */
            items: components["schemas"]["ProfileResponse"][];
            page: components["schemas"]["CursorPageInfo"];
        };
        /** CursorPage[PublicUserResponse] */
        CursorPage_PublicUserResponse_: {
            /** Items */
            items: components["schemas"]["PublicUserResponse"][];
            page: components["schemas"]["CursorPageInfo"];
        };
        /**
         * ErrorCode
         * @description Every code an `Arena64Error` can carry. Additive only — see
         *     `Arena64Error`'s own note on why: removing or renaming a member breaks
         *     every client that branches on it, including ones already deployed.
         *
         *     **When a module earns its own code, and when it doesn't.** A module's
         *     exception class always exists (`UserNotFound`, `IllegalMove`) — that is
         *     what server-side code branches on. A *new wire code* is added here only
         *     when a client must be able to behave differently for it, and the HTTP
         *     status plus the endpoint it came from are not enough to tell it apart.
         *     `UserNotFound` on `GET /users/{id}` needs no code of its own — `404` +
         *     `not_found` already says everything a client can act on. But a `409`
         *     from a registration form must say *which* field collided, so
         *     `USERNAME_ALREADY_EXISTS` and `EMAIL_ALREADY_EXISTS` are distinct
         *     codes. Without that rule this enum grows a member per exception class
         *     per module and stops being a useful thing to exhaustively switch over.
         * @enum {string}
         */
        ErrorCode: "internal_error" | "validation_error" | "domain_error" | "authentication_failed" | "not_found" | "conflict" | "permission_denied" | "precondition_failed" | "rule_violation" | "rate_limited" | "unsupported_engine_version" | "invalid_cursor" | "tournament_not_found" | "registration_not_open" | "registration_deadline_passed" | "tournament_full" | "already_registered" | "registration_not_found" | "invalid_tournament_state" | "infrastructure_error" | "transient_infrastructure_error" | "permanent_infrastructure_error" | "username_already_exists" | "email_already_exists" | "duplicate_friend_request" | "opposite_friend_request_pending" | "invalid_username" | "invalid_email" | "weak_password" | "invalid_credentials" | "inactive_account" | "account_locked" | "authentication_required" | "invalid_token" | "expired_token" | "invalid_session" | "session_expired" | "invalid_verification_token" | "invalid_reset_token" | "avatar_too_large" | "queue_cooldown_active";
        /**
         * ErrorResponse
         * @description The only shape an Arena64 error takes on the wire: a safe message and
         *     a stable, machine-readable code (services.md §7.2 rule 4) — never a
         *     stack trace, SQL, or an internal identifier.
         *
         *     Deliberately not `app.core.responses.ApiResponse` — nesting an error
         *     under `data` would make a client check "did this succeed" by inspecting
         *     the body instead of the HTTP status, the one signal that is never
         *     ambiguous. `request_id` and `correlation_id` mirror `ResponseMeta`'s
         *     fields directly rather than reusing it, so an error body never implies
         *     it carries a `data` field it doesn't have.
         */
        ErrorResponse: {
            code: components["schemas"]["ErrorCode"];
            /** Message */
            message: string;
            /** Request Id */
            request_id?: string | null;
            /** Correlation Id */
            correlation_id?: string | null;
        };
        /**
         * ForgotPasswordRequest
         * @description The `POST /auth/password/forgot` body.
         *
         *     Takes an address rather than requiring a bearer token, because the
         *     person who needs this cannot sign in — requiring authentication would
         *     make the endpoint useless to exactly its intended caller.
         *
         *     `EmailField` reuses `users`' validator, so the address is normalised
         *     identically to registration and login. A malformed address is a
         *     field-level 422 and reveals nothing: an address that cannot be valid
         *     cannot belong to anyone.
         *
         *     Inherits `extra="forbid"`, so a client sending `user_id` alongside the
         *     address is rejected rather than having it ignored — which matters here
         *     more than on most bodies, since a `user_id` a caller could supply is
         *     the shape of a request that resets somebody else's password.
         * @example {
         *       "email": "player@example.com"
         *     }
         */
        ForgotPasswordRequest: {
            /**
             * Email
             * @description The address the account was registered with.
             */
            email: string;
        };
        /**
         * FriendCountResponse
         * @description How many friends you have.
         *
         *     Its own shape rather than a bare integer, because a JSON response body
         *     that is a number has nowhere to grow: A64-013.4's management surface
         *     will plausibly want a pending-request count beside this one, and adding
         *     a field to an object is additive while changing `42` into `{"total": 42}`
         *     is not.
         * @example {
         *       "total": 7
         *     }
         */
        FriendCountResponse: {
            /**
             * Total
             * @description Your current number of friends. Counts live friendships only — a friendship that ended is not included, in either direction.
             * @example 7
             */
            total: number;
        };
        /**
         * FriendRequestResponse
         * @description One friend request, with the other party's public profile.
         * @example {
         *       "created_at": "2026-08-01T12:00:00Z",
         *       "id": "019fb9ea-1b2c-7def-8a45-90ab12cd34ef",
         *       "player": {
         *         "country": "GB",
         *         "display_name": "Player One",
         *         "id": "019fb9ea-0a0c-7cec-9c5f-402727c31a96",
         *         "is_online": true,
         *         "joined_at": "2026-07-01T09:30:00Z",
         *         "language": "en",
         *         "ratings": {
         *           "blitz": {
         *             "games_played": 0,
         *             "is_provisional": true,
         *             "rating": 1500
         *           },
         *           "classic": {
         *             "games_played": 0,
         *             "is_provisional": true,
         *             "rating": 1500
         *           },
         *           "rapid": {
         *             "games_played": 0,
         *             "is_provisional": true,
         *             "rating": 1500
         *           }
         *         },
         *         "statistics": {
         *           "best_win_streak": 0,
         *           "current_rating": 1500,
         *           "current_streak": 0,
         *           "draws": 0,
         *           "games_played": 0,
         *           "highest_rating": 1500,
         *           "losses": 0,
         *           "win_rate": 0,
         *           "wins": 0
         *         },
         *         "username": "player_one"
         *       },
         *       "status": "pending"
         *     }
         */
        FriendRequestResponse: {
            /**
             * Id
             * Format: uuid
             * @description The request identifier. Pass it to the accept, decline and cancel endpoints — it is the only handle a client needs, and it is scoped to the two players involved.
             * @example 019fb9ea-1b2c-7def-8a45-90ab12cd34ef
             */
            id: string;
            /**
             * @description Where the request is in its lifecycle. Both list endpoints return only `pending` today; the other values appear in the response to the action that produced them — `accepted`, `declined` or `cancelled`.
             * @example pending
             */
            status: components["schemas"]["FriendRequestStatus"];
            /** @description The **other** party — the sender on an incoming request, the recipient on an outgoing one. Identical in shape and in privacy behaviour to `GET /profiles/{username}`: a hidden country, record or presence is `null` here for the same reasons and says as little about why. */
            player: components["schemas"]["ProfileResponse"];
            /**
             * Created At
             * Format: date-time
             * @description When the request was sent, UTC.
             * @example 2026-08-01T12:00:00Z
             */
            created_at: string;
            /**
             * Responded At
             * @description When the request was resolved, UTC. `null` while it is `pending` — the two always agree, because a database CHECK enforces the pairing.
             * @example null
             */
            responded_at?: string | null;
        };
        /**
         * FriendRequestStatus
         * @description Where a request is in domain-model.md §8.1's lifecycle.
         *
         *     A `StrEnum` so the stored value, the wire value and the Python member
         *     are one string — the choice `VisibilityLevel` and `BoardTheme` make, for
         *     the same reason.
         *
         *     **Six members, three of which nothing produces yet.** `EXPIRED` and
         *     `VOIDED` are declared now so the PostgreSQL enum type contains them
         *     before anything needs to write one; adding a value to an enum used by an
         *     indexed column on a live table is a migration nobody should have to
         *     schedule to ship a feature. See this module's docstring.
         * @enum {string}
         */
        FriendRequestStatus: "pending" | "accepted" | "declined" | "cancelled" | "expired" | "declined_by_block";
        /**
         * FriendResponse
         * @description One friend, with the date the friendship began.
         * @example {
         *       "friends_since": "2026-08-01T12:00:00Z",
         *       "player": {
         *         "country": "GB",
         *         "display_name": "Player One",
         *         "id": "019fb9ea-0a0c-7cec-9c5f-402727c31a96",
         *         "is_online": true,
         *         "joined_at": "2026-07-01T09:30:00Z",
         *         "language": "en",
         *         "last_seen": "2026-08-01T15:44:00Z",
         *         "ratings": {
         *           "blitz": {
         *             "games_played": 0,
         *             "is_provisional": true,
         *             "rating": 1500
         *           },
         *           "classic": {
         *             "games_played": 0,
         *             "is_provisional": true,
         *             "rating": 1500
         *           },
         *           "rapid": {
         *             "games_played": 0,
         *             "is_provisional": true,
         *             "rating": 1500
         *           }
         *         },
         *         "statistics": {
         *           "best_win_streak": 0,
         *           "current_rating": 1500,
         *           "current_streak": 0,
         *           "draws": 0,
         *           "games_played": 0,
         *           "highest_rating": 1500,
         *           "losses": 0,
         *           "win_rate": 0,
         *           "wins": 0
         *         },
         *         "username": "player_one"
         *       }
         *     }
         */
        FriendResponse: {
            /** @description Your friend's public profile — identical in shape to `GET /profiles/{username}`. Fields they have restricted to friends **are visible here**, because you are one; the same profile read by a stranger hides them. */
            player: components["schemas"]["ProfileResponse"];
            /**
             * Friends Since
             * Format: date-time
             * @description When the friendship began, UTC — the instant the friend request was accepted, and the same instant that request records as its response.
             * @example 2026-08-01T12:00:00Z
             */
            friends_since: string;
        };
        /**
         * FriendshipDetailsResponse
         * @description One friendship, inspected — A64-013.4's `GET /friends/{player_id}`.
         *
         *     Deliberately the **same two fields** as `FriendResponse` above, and not
         *     merged with it. They answer different questions and will diverge: a list
         *     item is what a row renders, while this is what a relationship page
         *     shows, and the first field to arrive on one and not the other is
         *     whatever A64-013.4's successors add — a mutual-friend count, shared
         *     match history, a "friends for 3 months" badge.
         *
         *     Merging them now would save five lines and would make the first
         *     divergence a breaking change to the list.
         *
         *     ## What is deliberately absent
         *
         *     No friendship id, no `player_low_id`, no `player_high_id`, no
         *     `ended_at`. A64-013.4: "do NOT expose internal database fields." The
         *     canonical ordering is a storage concern (DB-12) that means nothing to a
         *     client; a friendship id is a second name for a relationship already
         *     addressed by the other player's id; and this endpoint only ever
         *     describes a live friendship, so an end date would be `null` on every
         *     response.
         *
         *     **No mutual friend count**, which the read model behind this *does*
         *     carry. A64-013.4 scopes mutual counts to "repository/service only", so
         *     the number is computed, tested and unpublished — the field appears here
         *     the day a UX surface asks for it, additively.
         * @example {
         *       "friends_since": "2026-08-01T12:00:00Z",
         *       "player": {
         *         "country": "GB",
         *         "display_name": "Player One",
         *         "id": "019fb9ea-0a0c-7cec-9c5f-402727c31a96",
         *         "is_online": true,
         *         "joined_at": "2026-07-01T09:30:00Z",
         *         "language": "en",
         *         "last_seen": "2026-08-01T15:44:00Z",
         *         "ratings": {
         *           "blitz": {
         *             "games_played": 0,
         *             "is_provisional": true,
         *             "rating": 1500
         *           },
         *           "classic": {
         *             "games_played": 0,
         *             "is_provisional": true,
         *             "rating": 1500
         *           },
         *           "rapid": {
         *             "games_played": 0,
         *             "is_provisional": true,
         *             "rating": 1500
         *           }
         *         },
         *         "statistics": {
         *           "best_win_streak": 0,
         *           "current_rating": 1500,
         *           "current_streak": 0,
         *           "draws": 0,
         *           "games_played": 0,
         *           "highest_rating": 1500,
         *           "losses": 0,
         *           "win_rate": 0,
         *           "wins": 0
         *         },
         *         "username": "player_one"
         *       }
         *     }
         */
        FriendshipDetailsResponse: {
            /** @description Your friend's public profile — identical in shape to `GET /profiles/{username}`. Fields they have restricted to friends **are visible here**, because you are one. */
            player: components["schemas"]["ProfileResponse"];
            /**
             * Friends Since
             * Format: date-time
             * @description When the friendship began, UTC — the instant the friend request was accepted, and the same instant that request records as its response.
             * @example 2026-08-01T12:00:00Z
             */
            friends_since: string;
        };
        /**
         * GameplayPreferencesResponse
         * @description The gameplay group, complete.
         */
        GameplayPreferencesResponse: {
            /**
             * @description The board's colour scheme.
             * @example classic
             */
            board_theme: components["schemas"]["BoardTheme"];
            /**
             * @description Which piece artwork is drawn.
             * @example classic
             */
            piece_set: components["schemas"]["PieceSet"];
            /**
             * Confirm Move
             * @description Whether a move needs a second confirming action.
             * @example false
             */
            confirm_move: boolean;
            /**
             * Show Coordinates
             * @description Whether file and rank labels are drawn.
             * @example true
             */
            show_coordinates: boolean;
            /**
             * @description How fast a piece slides to its square.
             * @example normal
             */
            animation_speed: components["schemas"]["AnimationSpeed"];
        };
        /**
         * GameplayPreferencesUpdate
         * @description The `gameplay` group of a `PATCH /profile/preferences` body.
         *
         *     Every setting optional; send only what changes. Sending this group does
         *     **not** reset the settings inside it that you did not name.
         * @example {
         *       "board_theme": "wood",
         *       "confirm_move": true
         *     }
         */
        GameplayPreferencesUpdate: {
            /**
             * @description The board's colour scheme. One of `classic`, `wood`, `marble`, `midnight`. Default: `classic`.
             * @example wood
             */
            board_theme?: components["schemas"]["BoardTheme"] | null;
            /**
             * @description Which piece artwork is drawn. One of `classic`, `modern`, `neo`. Default: `classic`.
             * @example modern
             */
            piece_set?: components["schemas"]["PieceSet"] | null;
            /**
             * Confirm Move
             * @description Whether a move needs a second confirming action before it is sent. Default: `false` — a confirmation step protects against a mis-drag but costs a click on every move that was fine.
             * @example true
             */
            confirm_move?: boolean | null;
            /**
             * Show Coordinates
             * @description Whether file and rank labels are drawn around the board. Default: `true`.
             * @example false
             */
            show_coordinates?: boolean | null;
            /**
             * @description How fast a piece slides to its square. One of `instant`, `fast`, `normal`, `slow`. Default: `normal`. `instant` disables motion entirely and is an accessibility setting, not a fourth speed.
             * @example fast
             */
            animation_speed?: components["schemas"]["AnimationSpeed"] | null;
        };
        /** HTTPValidationError */
        HTTPValidationError: {
            /** Detail */
            detail?: components["schemas"]["ValidationError"][];
        };
        /**
         * ImageDimensions
         * @description The pixel size of a stored rendition.
         */
        ImageDimensions: {
            /**
             * Width
             * @description Width in pixels.
             * @example 512
             */
            width: number;
            /**
             * Height
             * @description Height in pixels.
             * @example 341
             */
            height: number;
        };
        /**
         * JoinQueueRequest
         * @description What `POST /matchmaking/queue` accepts.
         *
         *     **Three fields, and none of them is a rating.** QT-2 makes the rating
         *     snapshot the platform's to record, and a client-supplied one would be a
         *     self-reported skill level on the endpoint that decides who you play —
         *     the single most valuable field on the platform to lie about.
         *
         *     Nor is there a `player_id`: the account comes from the access token, so
         *     queueing as somebody else is not something this API can express.
         *
         *     `variant` is a `ProductVariant` and not the engine's `BoardVariant`, so
         *     the generated OpenAPI schema lists only variants a player may choose.
         *     The English 8x8 fixture the engine uses for its perft oracle
         *     (`specs/game-engine/audit.md` §9) is not an accepted value and does not
         *     appear in the document — a validator on a wider enum would have kept it
         *     out of responses and left it advertised as input.
         */
        JoinQueueRequest: {
            /**
             * @description Which rules to play under. Only the variants Arena64 offers are accepted — the engine can play others, and they are not on the menu.
             * @default russian_8x8
             */
            variant: components["schemas"]["ProductVariant"];
            /** @description Which pool to wait in. `ranked` moves your rating when the match finishes; `casual` does not. */
            queue_type: components["schemas"]["QueueType"];
            /**
             * @description Where to look for an opponent. `global` — the default — means anywhere, and is the right answer unless you would rather wait longer for a shorter round trip.
             * @default global
             */
            region: components["schemas"]["Region"];
        };
        /**
         * LeaderboardEntryResponse
         * @description One row of the ladder.
         */
        LeaderboardEntryResponse: {
            /**
             * Player Id
             * Format: uuid
             */
            player_id: string;
            /** Rating */
            rating: number;
            /** Deviation */
            deviation: number;
            /** Games Played */
            games_played: number;
            /**
             * Is Provisional
             * @description Provisional players are ranked and shown, never hidden.
             */
            is_provisional: boolean;
        };
        /**
         * LeaderboardNeighbourhoodResponse
         * @description Where one player stands, and who is next to them.
         *
         *     `rank` counts from 1 and is **unique**: the ladder's ordering is total,
         *     so no two players share a position. That is deliberately unlike a
         *     tournament's placement, where a shared tier is the product rule.
         */
        LeaderboardNeighbourhoodResponse: {
            /** Variant */
            variant: string;
            /** Speed Class */
            speed_class: string;
            /** Rank */
            rank: number;
            entry: components["schemas"]["LeaderboardEntryResponse"];
            /**
             * Above
             * @description Rows immediately better, nearest last.
             */
            above: components["schemas"]["LeaderboardEntryResponse"][];
            /**
             * Below
             * @description Rows immediately worse, nearest first.
             */
            below: components["schemas"]["LeaderboardEntryResponse"][];
        };
        /**
         * LeaderboardResponse
         * @description One page of one key's ladder, best first.
         */
        LeaderboardResponse: {
            /** Variant */
            variant: string;
            /** Speed Class */
            speed_class: string;
            /** Entries */
            entries: components["schemas"]["LeaderboardEntryResponse"][];
            /**
             * Next Cursor
             * @description Opaque; send it back unread for the next page.
             */
            next_cursor?: string | null;
        };
        /** LivenessResponse */
        LivenessResponse: {
            /**
             * Status
             * @default ok
             */
            status: string;
        };
        /**
         * Locale
         * @description The three locales the platform supports (task A64-008; mirrors
         *     `apps/web/src/i18n/routing.ts`'s `routing.locales` and anticipates
         *     `database.md`'s `reference.locale` table). `EN` is first because it is
         *     the default — `app.common.locale.resolve_locale` falls back to it.
         * @enum {string}
         */
        Locale: "en" | "ru" | "uz";
        /**
         * LocalePreferencesResponse
         * @description The locale group, complete.
         */
        LocalePreferencesResponse: {
            /**
             * @description Your interface language.
             * @example en
             */
            preferred_language: components["schemas"]["Locale"];
            /**
             * Timezone
             * @description Your IANA timezone name.
             * @example UTC
             */
            timezone: string;
        };
        /**
         * LocalePreferencesUpdate
         * @description The `locale` group of a `PATCH /profile/preferences` body.
         *
         *     **This is the only place either of these can be changed.** Both were
         *     editable through `PATCH /profile` until A64-012.5, which removed them
         *     there — a field with two writable endpoints is a field whose validation
         *     and audit trail differ depending on which one a client used.
         * @example {
         *       "preferred_language": "uz"
         *     }
         */
        LocalePreferencesUpdate: {
            /**
             * @description Interface language. One of `en`, `ru`, `uz`. Default: `en`.
             * @example uz
             */
            preferred_language?: components["schemas"]["Locale"] | null;
            /**
             * Timezone
             * @description IANA timezone name — `Asia/Tashkent`, `Europe/London`, `UTC`. Validated against this system's IANA database. Never a UTC offset: an offset is a fact about one instant, not a timezone, and would break the moment daylight-saving rules applied. Default: `UTC`.
             * @example Asia/Tashkent
             */
            timezone?: string | null;
        };
        /**
         * LoginRequest
         * @description The `POST /auth/login` body.
         *
         *     `email` reuses `users`' `EmailField`, so the address is trimmed and
         *     folded by exactly the validator registration used — "  Alice@Example.COM "
         *     and "alice@example.com" are the same account, which they must be or
         *     people simply cannot log in from a phone keyboard that capitalises.
         *
         *     A malformed address is rejected here with a field-level 422, which is
         *     the right feedback for a form and reveals nothing: an address that
         *     cannot be valid cannot belong to anyone. Every other caller is covered
         *     by `AuthenticationService`, which routes the same failure into the
         *     generic `InvalidCredentials` path.
         *
         *     `password` is a `SecretStr` — see `RegisterRequest` on why that
         *     matters at this layer specifically.
         *
         *     Inherits `extra="forbid"` from `BaseRequestDTO`. A client sending
         *     `is_active` or `user_id` alongside its credentials is rejected rather
         *     than silently ignored.
         * @example {
         *       "email": "player@example.com",
         *       "password": "CorrectHorse1!"
         *     }
         */
        LoginRequest: {
            /** Email */
            email: string;
            /**
             * Password
             * Format: password
             */
            password: string;
        };
        /**
         * MatchHistoryEntryResponse
         * @description One finished match, as a list renders it.
         *
         *     `opponent_id` is present only when the viewer played — it is the field
         *     that makes a personal history readable, and it is meaningless when a
         *     stranger reads somebody else's record.
         */
        MatchHistoryEntryResponse: {
            /**
             * Match Id
             * Format: uuid
             */
            match_id: string;
            /** Variant */
            variant: string;
            /**
             * Speed Class
             * @description The rating key's speed class, when the match recorded one.
             */
            speed_class?: string | null;
            /** Rated */
            rated: boolean;
            /**
             * Engine Version
             * @description Replay is refused for versions this build cannot reproduce.
             */
            engine_version: number;
            /**
             * Light Player Id
             * Format: uuid
             */
            light_player_id: string;
            /**
             * Dark Player Id
             * Format: uuid
             */
            dark_player_id: string;
            /**
             * Opponent Id
             * @description The other player, when the viewer is a participant.
             */
            opponent_id?: string | null;
            /** Outcome */
            outcome: string | null;
            /** Termination Reason */
            termination_reason: string | null;
            /** Winner */
            winner: string | null;
            /** Ply Number */
            ply_number: number;
            /**
             * Started At
             * Format: date-time
             */
            started_at: string;
            /** Ended At */
            ended_at: string | null;
        };
        /** MatchHistoryResponse */
        MatchHistoryResponse: {
            /** Entries */
            entries: components["schemas"]["MatchHistoryEntryResponse"][];
            /**
             * Next Cursor
             * @description Opaque. Send it back as `after`; `null` on the last page.
             */
            next_cursor?: string | null;
        };
        /**
         * MatchRecordStatus
         * @description Where a paired contest is in the platform's half of its life.
         *
         *     Four states, which is what A64-015.4 §4 asks for and no more. Each is
         *     reached by a different actor, which is why none of them collapses into
         *     another:
         *
         *         pending_acceptance  a pairing produced it; two people must answer
         *         active              both answered yes; the match may be played
         *         cancelled           one answered no
         *         expired             nobody answered in time
         * @enum {string}
         */
        MatchRecordStatus: "pending_acceptance" | "active" | "completed" | "cancelled" | "expired";
        /**
         * MatchReplayResponse
         * @description A whole finished game, reconstructed.
         */
        MatchReplayResponse: {
            /**
             * Match Id
             * Format: uuid
             */
            match_id: string;
            /** Variant */
            variant: string;
            /** Engine Version */
            engine_version: number;
            /** Opening */
            opening: components["schemas"]["PlacedPieceResponse"][];
            /** Plies */
            plies: components["schemas"]["ReplayPlyResponse"][];
            /** Outcome */
            outcome: string | null;
            /** Termination Reason */
            termination_reason: string | null;
            /** Winner */
            winner: string | null;
        };
        /**
         * MyProfileResponse
         * @description The account holder's own profile, as returned by both endpoints.
         *
         *     One shape for `GET /profile/me` and `PATCH /profile`, because both
         *     answer the same question — *what is my profile now* — and a client's
         *     handling is identical. Returning the full profile from the PATCH also
         *     means the response carries **normalised** values: a display name sent
         *     padded comes back trimmed, a country sent `uz` comes back `UZ`. A
         *     client that echoed its own request would render something the platform
         *     did not store.
         *
         *     Carries no `email`, no `is_verified` and no `is_active`. Those are
         *     `auth`'s and are served by `GET /auth/me`; a second copy here would be
         *     a second thing to keep in step.
         * @example {
         *       "bio": "Blitz player. Occasionally studies the endgame.",
         *       "country": "UZ",
         *       "display_name": "Жанибек Алиев",
         *       "id": "019fb9ea-0a0c-7cec-9c5f-402727c31a96",
         *       "joined_at": "2026-08-01T12:00:00Z",
         *       "statistics": {
         *         "best_win_streak": 0,
         *         "current_rating": 1500,
         *         "current_streak": 0,
         *         "draws": 0,
         *         "games_played": 0,
         *         "highest_rating": 1500,
         *         "losses": 0,
         *         "win_rate": 0,
         *         "wins": 0
         *       },
         *       "username": "player_one"
         *     }
         */
        MyProfileResponse: {
            /**
             * Id
             * Format: uuid
             * @description Your player identifier — stable and public.
             * @example 019fb9ea-0a0c-7cec-9c5f-402727c31a96
             */
            id: string;
            /**
             * Username
             * @description Your handle. **Read-only** — username changes are not part of this API. Renaming has to record the previous handle and hold it in a reuse cooldown, so it will arrive as its own endpoint.
             * @example player_one
             */
            username: string;
            /**
             * Display Name
             * @description The name shown beside your avatar, trimmed. `null` when unset.
             * @example Жанибек Алиев
             */
            display_name: string | null;
            /**
             * Bio
             * @description Your self-description, plain text. `null` when unset.
             * @example Blitz player. Occasionally studies the endgame.
             */
            bio: string | null;
            /**
             * Country
             * @description ISO 3166-1 alpha-2, upper-cased. `null` when unset.
             * @example UZ
             */
            country: string | null;
            /**
             * Avatar Url
             * @description Your avatar at up to 512px, or `null`. Managed at `/profile/avatar`.
             * @example null
             */
            avatar_url: string | null;
            /**
             * Thumbnail Url
             * @description The 128px rendition, or `null`.
             * @example null
             */
            thumbnail_url: string | null;
            /**
             * Joined At
             * Format: date-time
             * @description When your account was created, UTC.
             * @example 2026-08-01T12:00:00Z
             */
            joined_at: string;
            /**
             * Is Online
             * @description Whether **you** are shown as connected right now.
             *
             *     **Never redacted.** `show_online_status` governs what strangers see on `GET /profiles/{username}`, not what you see of yourself — a control that hid your own presence from you would be one you could never verify you had set.
             *
             *     `null` means the platform has no current observation of you, not that it is hidden: presence decays on a timer, so a client that has not signed in or refreshed recently reads as `null`.
             * @example true
             */
            is_online?: boolean | null;
            /**
             * Last Seen
             * @description When **you** were last observed online, UTC. **Never redacted** — `show_last_seen` governs strangers, not you.
             *
             *     Recorded by authentication since A64-013.6: signing in and refreshing a token both stamp this, and signing out of every device stamps it once more. `null` only where nothing has been observed — render it as 'unknown', never as a date.
             * @example 2026-08-01T12:00:00Z
             */
            last_seen?: string | null;
            /** @description Your competitive record. **Always present, never `null`** — `show_statistics` governs what *strangers* see on `GET /profiles/{username}`, not what you see of yourself. A privacy control that hid a setting from the person who set it would be one nobody could verify they had applied. */
            statistics: components["schemas"]["StatisticsResponse"];
        };
        /**
         * OpponentPreview
         * @description Just enough of the other player to render a match card.
         *
         *     Three fields, and the same three `users.public.UserSummary` calls "what
         *     a list, a search result, or a future match card needs". This is that
         *     match card, and the fields it omits — email, verification state, join
         *     date, country, bio — are the ones that would make it a privacy question
         *     rather than a rendering one.
         */
        OpponentPreview: {
            /**
             * Player Id
             * Format: uuid
             */
            player_id: string;
            /** Username */
            username: string;
            /** Display Name */
            display_name: string | null;
        };
        /**
         * PendingMatchResponse
         * @description One match, as one of its two participants sees it.
         *
         *     Every field is named from the **reader's** seat rather than by side —
         *     `your_side`, `you_accepted`, `opponent_accepted` — so a client cannot
         *     render the wrong half by picking the wrong field, and so nothing here
         *     reveals which physical row a value came from.
         */
        PendingMatchResponse: {
            /**
             * Match Id
             * Format: uuid
             */
            match_id: string;
            /** @description `pending_acceptance` while the offer is open, and the state it settled into once it is not — `active` when both of you accepted, `cancelled` when somebody declined, `expired` when the window closed. A client that polls after answering sees the outcome rather than a `404`. */
            status: components["schemas"]["MatchRecordStatus"];
            /** @description Which side you were assigned. `light` moves first. */
            your_side: components["schemas"]["PlayerSide"];
            /** @description Who you were paired with. `null` when that account can no longer be rendered — deactivated between the pairing and this read — which is the same answer every other endpoint on this platform gives for a withdrawn account. */
            opponent: components["schemas"]["OpponentPreview"] | null;
            variant: components["schemas"]["ProductVariant"];
            /**
             * Rated
             * @description Whether finishing this match will move your rating.
             */
            rated: boolean;
            /**
             * Acceptance Deadline
             * Format: date-time
             * @description When this offer stops being honoured. An instant rather than a countdown, so a slow response cannot make a client's timer wrong. An answer that arrives after it is refused.
             */
            acceptance_deadline: string;
            /** You Accepted */
            you_accepted: boolean;
            /**
             * Opponent Accepted
             * @description Whether the other player has answered yes. Never says whether they have *declined* — a decline settles the match, and `status` is where that is reported.
             */
            opponent_accepted: boolean;
            /**
             * Created At
             * Format: date-time
             * @description When the pairing produced this match.
             */
            created_at: string;
        };
        /**
         * PieceSet
         * @description Which piece artwork the client draws.
         * @enum {string}
         */
        PieceSet: "classic" | "modern" | "neo";
        /** PlacedPieceResponse */
        PlacedPieceResponse: {
            /** Square */
            square: string;
            /** Side */
            side: string;
            /** Rank */
            rank: string;
        };
        /**
         * PlayerRatingsResponse
         * @description Every key a player has a standing in, played or not.
         *
         *     Ordered by speed class in the enum's own order — bullet to classical —
         *     so a client renders a stable row of tabs rather than one that reshuffles
         *     when a rating appears.
         */
        PlayerRatingsResponse: {
            /**
             * Player Id
             * Format: uuid
             */
            player_id: string;
            /** Ratings */
            ratings: components["schemas"]["app__modules__rating__presentation__schemas__ratings__RatingResponse"][];
        };
        /**
         * PlayerSide
         * @description Which player a piece belongs to.
         *
         *     "Light" and "dark", not "white" and "black". The pieces on a draughts
         *     board are not white and black in any set anyone owns — they are cream
         *     and red, or buff and green — and the two rules that actually depend on
         *     side are about *direction of travel* and *who moves first*, neither of
         *     which is a colour. Every board theme the client ships renames these;
         *     the domain should not be tied to one of them.
         * @enum {string}
         */
        PlayerSide: "light" | "dark";
        /**
         * PlayerTournamentResponse
         * @description One tournament a player entered — §12.
         */
        PlayerTournamentResponse: {
            tournament: components["schemas"]["TournamentResponse"];
            /** Seed Number */
            seed_number?: number | null;
            /** Final Rank */
            final_rank?: number | null;
            /** Final Status */
            final_status?: string | null;
        };
        /**
         * PlayerTournamentsResponse
         * @description One page of a player's tournament history, newest first.
         */
        PlayerTournamentsResponse: {
            /** Entries */
            entries: components["schemas"]["PlayerTournamentResponse"][];
            /**
             * Next Cursor
             * @description Opaque; send it back unread for the next page.
             */
            next_cursor?: string | null;
        };
        /**
         * PreferencesResponse
         * @description Your preferences, as returned by both preference endpoints.
         *
         *     **Always complete.** Every group is present and every setting inside it
         *     has a value, even for an account that has never opened a settings
         *     screen — the stored document is empty for such an account and the
         *     domain fills every absent key with its default. A client renders every
         *     control from this one response and never needs to know what a default
         *     is.
         *
         *     One shape for `GET` and `PATCH`, because both answer *what are my
         *     settings now*, and returning the complete set from the PATCH means a
         *     client renders the result rather than applying an optimistic update and
         *     drifting from what was stored.
         *
         *     **Never public.** No anonymous read path returns this or any part of
         *     it — see `PreferencesView`.
         * @example {
         *       "gameplay": {
         *         "animation_speed": "normal",
         *         "board_theme": "classic",
         *         "confirm_move": false,
         *         "piece_set": "classic",
         *         "show_coordinates": true
         *       },
         *       "locale": {
         *         "preferred_language": "en",
         *         "timezone": "UTC"
         *       }
         *     }
         */
        PreferencesResponse: {
            gameplay: components["schemas"]["GameplayPreferencesResponse"];
            locale: components["schemas"]["LocalePreferencesResponse"];
        };
        /**
         * PreferencesUpdateRequest
         * @description The `PATCH /profile/preferences` body.
         *
         *     Both groups optional. An omitted group is left entirely alone — this is
         *     what makes `{"locale": {"timezone": "UTC"}}` a timezone change rather
         *     than a reset of five gameplay settings.
         *
         *     `{}` is legal and changes nothing, which is the honest answer to an
         *     empty PATCH rather than an error: the request is well formed and its
         *     effect is precisely none.
         * @example {
         *       "gameplay": {
         *         "animation_speed": "fast",
         *         "board_theme": "wood"
         *       }
         *     }
         * @example {
         *       "locale": {
         *         "preferred_language": "uz",
         *         "timezone": "Asia/Tashkent"
         *       }
         *     }
         * @example {
         *       "gameplay": {
         *         "animation_speed": "instant",
         *         "board_theme": "midnight",
         *         "confirm_move": true,
         *         "piece_set": "neo",
         *         "show_coordinates": false
         *       },
         *       "locale": {
         *         "preferred_language": "ru",
         *         "timezone": "Europe/Moscow"
         *       }
         *     }
         */
        PreferencesUpdateRequest: {
            /** @description Board appearance and behaviour. Omit to leave the whole group alone. */
            gameplay?: components["schemas"]["GameplayPreferencesUpdate"] | null;
            /** @description Language and timezone. Omit to leave the whole group alone. */
            locale?: components["schemas"]["LocalePreferencesUpdate"] | null;
        };
        /**
         * PrivacySettingsResponse
         * @description Your privacy settings, as returned by both privacy endpoints.
         *
         *     One shape for `GET` and `PATCH`, because both answer the same question
         *     — *what are my settings now* — and returning the complete set from the
         *     PATCH means a client renders every toggle from the response rather than
         *     applying an optimistic update and drifting from what was stored.
         *
         *     **Only ever returned to the account holder.** Nothing on the public
         *     profile carries these flags: `GET /profiles/{username}` shows a `null`
         *     where a hidden field would be and says nothing about why, because
         *     "this player hides their country" is itself the disclosure the setting
         *     declines to make.
         * @example {
         *       "activity": "everyone",
         *       "last_seen": "nobody",
         *       "online_status": "everyone",
         *       "show_activity": true,
         *       "show_country": true,
         *       "show_last_seen": false,
         *       "show_online_status": true,
         *       "show_statistics": true
         *     }
         */
        PrivacySettingsResponse: {
            /**
             * Show Country
             * @description Whether your country is shown on your public profile.
             * @example true
             */
            show_country: boolean;
            /**
             * @description Who may see the time you were last online, as `last_seen` on your public profile. `nobody` unless you changed it — the one setting here that is closed by default, because a published timestamp is a sleep schedule while 'online now' is momentary.
             * @example nobody
             */
            last_seen: components["schemas"]["VisibilityLevel"];
            /**
             * Show Last Seen
             * @deprecated
             * @description **Deprecated — read `last_seen` instead.** `true` only when `last_seen` is `everyone`; a friends-only setting reads as `false` here, because this field asks whether *anybody* may see it. Retained so clients written before A64-013.2 keep working, and removed no earlier than the next API version.
             * @example false
             */
            show_last_seen: boolean;
            /**
             * Show Statistics
             * @description Whether your aggregate match record is shown. Does not affect your ratings, which are always public.
             * @example true
             */
            show_statistics: boolean;
            /**
             * @description Who may see that you are online, as `is_online` on your public profile. Governs the indicator only — `last_seen` governs the timestamp beside it, separately.
             * @example everyone
             */
            online_status: components["schemas"]["VisibilityLevel"];
            /**
             * Show Online Status
             * @deprecated
             * @description **Deprecated — read `online_status` instead.** See `show_last_seen`.
             * @example true
             */
            show_online_status: boolean;
            /**
             * @description Who may see your recent activity. **Stored but not yet applied anywhere** — no endpoint publishes activity, so changing this has no visible effect until a match history exists. Settable now so that the release which adds one is not also the release that decides who may read it.
             * @example everyone
             */
            activity: components["schemas"]["VisibilityLevel"];
            /**
             * Show Activity
             * @deprecated
             * @description **Deprecated — read `activity` instead.** See `show_last_seen`.
             * @example true
             */
            show_activity: boolean;
        };
        /**
         * PrivacySettingsUpdateRequest
         * @description The `PATCH /profile/privacy` body. Every flag optional; send only
         *     what changes.
         *
         *     Sending one flag does **not** reset the other four to their defaults.
         *     That is worth stating on the schema rather than only in the router,
         *     because it is the failure a client cannot detect: a request that
         *     silently re-enabled `show_last_seen` would look like a success and
         *     would publish a person's schedule.
         * @example {
         *       "show_last_seen": true
         *     }
         * @example {
         *       "show_activity": false,
         *       "show_country": false,
         *       "show_last_seen": false,
         *       "show_online_status": false,
         *       "show_statistics": false
         *     }
         */
        PrivacySettingsUpdateRequest: {
            /**
             * Show Country
             * @description Whether your country is shown on your public profile. Hidden means the field is `null` — indistinguishable from a player who never set one. Default: `true`.
             * @example true
             */
            show_country?: boolean | null;
            /**
             * @description Who may see the time you were last online. **Default: `nobody`** — the only one of the five that is closed by default. A last-seen time published continuously reveals a sleep schedule and a working pattern, and unlike the others it is observed rather than declared.
             *
             *     `friends` is accepted and stored, and currently behaves as `nobody`: friendships do not exist until A64-013.3, so no viewer can be a friend yet. That is the safe direction, and it is stated rather than silently rejected so a client can set the value it means today.
             * @example nobody
             */
            last_seen?: components["schemas"]["VisibilityLevel"] | null;
            /**
             * Show Last Seen
             * @deprecated
             * @description **Deprecated — send `last_seen` instead.** Accepted for clients written before A64-013.2: `true` sets `everyone`, `false` sets `nobody`. Sending both this and `last_seen` is a `422`, because two incompatible intentions in one request must not be resolved by a coin flip.
             * @example false
             */
            show_last_seen?: boolean | null;
            /**
             * Show Statistics
             * @description Whether your aggregate match record — games, wins, losses, draws, win rate — is shown. Hidden means `statistics` is `null`, never zeroes. **Does not cover your ratings**, which are always public: a rating is what pairing is computed from. Default: `true`.
             * @example true
             */
            show_statistics?: boolean | null;
            /**
             * @description Who may see that you are online right now. Honoured on `GET /profiles/{username}`, where a viewer outside this audience reads `is_online: null` — indistinguishable from a player nobody has observed. Presence itself has been recorded since A64-013.6. Default: `everyone`.
             * @example everyone
             */
            online_status?: components["schemas"]["VisibilityLevel"] | null;
            /**
             * Show Online Status
             * @deprecated
             * @description **Deprecated — send `online_status` instead.** See `show_last_seen`.
             * @example true
             */
            show_online_status?: boolean | null;
            /**
             * @description Who may see your recent activity — match history, activity feed. Stored and honoured ahead of the feature it governs, as `online_status` is. Default: `everyone`.
             * @example everyone
             */
            activity?: components["schemas"]["VisibilityLevel"] | null;
            /**
             * Show Activity
             * @deprecated
             * @description **Deprecated — send `activity` instead.** See `show_last_seen`.
             * @example true
             */
            show_activity?: boolean | null;
        };
        /**
         * ProductVariant
         * @description A rule set a player may choose to play.
         *
         *     Values are `BoardVariant` values, deliberately — one identifier for one
         *     rule set, so a stored ticket, a wire payload and an engine call all
         *     spell it the same way.
         * @enum {string}
         */
        ProductVariant: "russian_8x8";
        /**
         * ProfileResponse
         * @description A player as everyone else sees them.
         *
         *     Carries no `email`, no credential material and no account state
         *     (`is_verified`, `locked_until`, `is_active`). That is a property of the
         *     types this is built from rather than of remembering to omit them: the
         *     published DTO `profiles` receives from `users` has no such fields, so
         *     there is nothing here to strip.
         * @example {
         *       "country": "GB",
         *       "display_name": "Player One",
         *       "id": "019fb9ea-0a0c-7cec-9c5f-402727c31a96",
         *       "is_online": true,
         *       "joined_at": "2026-08-01T12:00:00Z",
         *       "language": "en",
         *       "ratings": {
         *         "blitz": {
         *           "games_played": 0,
         *           "is_provisional": true,
         *           "rating": 1500
         *         },
         *         "classic": {
         *           "games_played": 0,
         *           "is_provisional": true,
         *           "rating": 1500
         *         },
         *         "rapid": {
         *           "games_played": 0,
         *           "is_provisional": true,
         *           "rating": 1500
         *         }
         *       },
         *       "statistics": {
         *         "best_win_streak": 0,
         *         "current_rating": 1500,
         *         "current_streak": 0,
         *         "draws": 0,
         *         "games_played": 0,
         *         "highest_rating": 1500,
         *         "losses": 0,
         *         "win_rate": 0,
         *         "wins": 0
         *       },
         *       "username": "player_one"
         *     }
         * @example {
         *       "display_name": "Player One",
         *       "id": "019fb9ea-0a0c-7cec-9c5f-402727c31a96",
         *       "joined_at": "2026-08-01T12:00:00Z",
         *       "language": "en",
         *       "ratings": {
         *         "blitz": {
         *           "games_played": 0,
         *           "is_provisional": true,
         *           "rating": 1500
         *         },
         *         "classic": {
         *           "games_played": 0,
         *           "is_provisional": true,
         *           "rating": 1500
         *         },
         *         "rapid": {
         *           "games_played": 0,
         *           "is_provisional": true,
         *           "rating": 1500
         *         }
         *       },
         *       "username": "player_one"
         *     }
         */
        ProfileResponse: {
            /**
             * Id
             * Format: uuid
             * @description The player identifier — stable, public, and the value every other resource refers to this player by. Not an internal row id: sessions, tokens and credentials have their own identifiers and none of them is ever published.
             * @example 019fb9ea-0a0c-7cec-9c5f-402727c31a96
             */
            id: string;
            /**
             * Username
             * @description The handle, in the casing the player chose. Lookup is case-insensitive, so `/profiles/Alice` and `/profiles/alice` return this same profile.
             * @example player_one
             */
            username: string;
            /**
             * Display Name
             * @description A free-form name the player renders under. `null` when unset — clients should fall back to `username`.
             * @example Player One
             */
            display_name: string | null;
            /**
             * Avatar Url
             * @description URL of the player's avatar at up to 512px, or `null` when they have not set one. Carries a `?v=` cache-buster that changes on every upload and delete, so the URL may be treated as immutable and cached freely.
             * @example null
             */
            avatar_url: string | null;
            /**
             * Thumbnail Url
             * @description URL of the 128px rendition, for listings and match cards. `null` when the player has no avatar. Rendering a placeholder is the client's decision — only it knows the size and the surrounding design.
             * @example null
             */
            thumbnail_url: string | null;
            /**
             * Country
             * @description ISO 3166-1 alpha-2, upper-cased — `GB`, `UZ`. `null` when the player has not set one **or has chosen not to show it**. The two are deliberately indistinguishable: reporting which is which would answer the question the privacy setting exists to decline.
             * @example null
             */
            country: string | null;
            /**
             * @description The player's preferred interface language.
             * @example en
             */
            language: components["schemas"]["Locale"];
            /**
             * Bio
             * @description A short self-description — **plain text, at most 500 characters**. Markdown is not supported and must not be rendered: treat this as text, not markup. Already trimmed and free of control and bidirectional characters. `null` when unset, which today is every player.
             * @example null
             */
            bio?: string | null;
            /**
             * Joined At
             * Format: date-time
             * @description When the account was created, UTC.
             * @example 2026-08-01T12:00:00Z
             */
            joined_at: string;
            /**
             * Is Online
             * @description Whether the player is connected right now.
             *
             *     `true` while a connection is open. `false` means the platform saw them disconnect recently enough to still be able to say so. **`null` means nothing can be said** — and deliberately does not distinguish between a player who has hidden their presence (`show_online_status` off), a player nobody has observed, a presence record that has since expired, and presence being temporarily unavailable. Reporting which applies would answer the question the privacy setting exists to decline.
             *
             *     Render `null` as 'unknown', never as 'offline'. Presence is best-effort and decays on a timer, so a briefly stale value is expected rather than a fault.
             * @example true
             */
            is_online?: boolean | null;
            /**
             * Last Seen
             * @description When the player was last seen online, UTC.
             *
             *     **`null` unless the player has turned `show_last_seen` on**, which is the one privacy setting that is off by default — so this is `null` for most accounts however active they are. It is also `null` for a player nobody has observed, for a presence record that has expired, and when presence is unavailable, and those cases are deliberately indistinguishable from a player who opted out.
             *
             *     Never inferred from anything else the platform stores: an account's last write and a session's last token exchange are both something other than a person being present. Do not infer activity from its absence.
             * @example null
             */
            last_seen?: string | null;
            /** @description Current rating per category. Every category is always present; a player who has never played reports a provisional starting value rather than a missing key. **Ratings are always visible** — they are what pairing is computed from, and privacy settings do not cover them. */
            ratings: components["schemas"]["RatingsResponse"];
            /** @description Aggregate match record across every category, or `null` when the player has chosen not to show it. **Never zeroes for a hidden record** — a zeroed record is indistinguishable from a beginner's, which would mislead the opponent deciding whether to accept a challenge. Render `null` as 'not shown', not as 'no games'. */
            statistics: components["schemas"]["StatisticsResponse"] | null;
        };
        /**
         * ProfileUpdateRequest
         * @description The `PATCH /profile` body. Every field optional; send only what
         *     changes.
         *
         *     Bounds are declared here *as well as* in the domain validators, and the
         *     duplication is deliberate rather than an oversight: Pydantic's bound
         *     rejects a multi-megabyte biography while parsing, before the value
         *     reaches a validator that would have to materialise it first. The domain
         *     validator remains the authority — it is what a non-HTTP caller hits —
         *     and both read the same constants, so they cannot disagree about the
         *     number.
         * @example {
         *       "bio": "Blitz player. Occasionally studies the endgame.",
         *       "country": "UZ",
         *       "display_name": "Жанибек Алиев"
         *     }
         * @example {}
         */
        ProfileUpdateRequest: {
            /**
             * Display Name
             * @description The name shown beside your avatar. 3-50 characters, any script — surrounding whitespace is trimmed. Send `null` to remove it and fall back to your username.
             * @example Жанибек Алиев
             */
            display_name?: string | null;
            /**
             * Bio
             * @description A short self-description — **plain text**, at most 500 characters. Markdown is not supported and will not be rendered. Send `null` to remove it.
             * @example Blitz player. Occasionally studies the endgame.
             */
            bio?: string | null;
            /**
             * Country
             * @description ISO 3166-1 alpha-2 country code — `UZ`, `GB`, `RU`. Case-insensitive on input, stored upper-cased. Unassigned codes such as `XX` are rejected. Send `null` to remove it.
             * @example UZ
             */
            country?: string | null;
        };
        /**
         * PublicUserResponse
         * @description A player, by id, as anyone may see them.
         * @example {
         *       "display_name": "Player One",
         *       "id": "019fb9ea-0a0c-7cec-9c5f-402727c31a96",
         *       "username": "player_one"
         *     }
         */
        PublicUserResponse: {
            /**
             * Id
             * Format: uuid
             * @description The player identifier — stable, public, and the value every other resource refers to this player by.
             * @example 019fb9ea-0a0c-7cec-9c5f-402727c31a96
             */
            id: string;
            /**
             * Username
             * @description The handle, in the casing the player chose.
             * @example player_one
             */
            username: string;
            /**
             * Display Name
             * @description A free-form name the player renders under. `null` when unset — clients should fall back to `username`.
             * @example Player One
             */
            display_name: string | null;
            /**
             * Avatar Url
             * @description URL of the player's avatar at up to 512px, or `null`. Carries a `?v=` cache-buster that changes on every upload and delete.
             * @example null
             */
            avatar_url: string | null;
            /**
             * Thumbnail Url
             * @description URL of the 128px rendition, for listings and match cards. `null` when unset.
             * @example null
             */
            thumbnail_url: string | null;
        };
        /**
         * QueueStatus
         * @description A ticket's position in its lifecycle — see this module's docstring
         *     for the mapping to domain-model.md §10.2's seven.
         *
         *     Two of these are **live** and three are terminal, and nearly every rule
         *     in this module is a statement about that split: a live ticket occupies
         *     QT-1's uniqueness, carries no `resolved_at`, and is reported to its
         *     owner as "you are queued". A terminal one is finished and immutable.
         *
         *     The two live states differ in one way that matters: a **pairing scan
         *     reads only `waiting`**, so a reserved ticket is invisible to every
         *     other worker's next scan while it is being turned into a match.
         * @enum {string}
         */
        QueueStatus: "waiting" | "reserved" | "matched" | "cancelled" | "expired";
        /**
         * QueueTicketResponse
         * @description One ticket, as its owner sees it.
         *
         *     Every field is the player's own, so nothing here is gated by a privacy
         *     setting — unlike almost every other response on the platform. There is
         *     no endpoint that renders somebody *else's* ticket, and there is not
         *     meant to be: who is currently queueing is exactly the information that
         *     would let a player wait for a favourable pool.
         */
        QueueTicketResponse: {
            /**
             * Ticket Id
             * Format: uuid
             */
            ticket_id: string;
            variant: components["schemas"]["ProductVariant"];
            queue_type: components["schemas"]["QueueType"];
            region: components["schemas"]["Region"];
            status: components["schemas"]["QueueStatus"];
            /**
             * Rating Snapshot
             * @description The rating this ticket was entered with. Fixed at entry — a rating that changes while you wait does not move your place in the pool.
             */
            rating_snapshot: number;
            /**
             * Entered At
             * Format: date-time
             */
            entered_at: string;
            /**
             * Expires At
             * Format: date-time
             * @description When this ticket stops being honoured. An instant rather than a countdown, so a slow response cannot make a client's timer wrong.
             */
            expires_at: string;
            /**
             * Waiting
             * @description How many players are currently waiting in the same pool, including you. A reading rather than a promise — it changes continuously, and it is not a position in a line.
             */
            waiting: number;
        };
        /**
         * QueueType
         * @description Whether a game in this pool moves a rating.
         *
         *     The split that changes what a match *means* rather than how it is
         *     played: a rated game moves a permanent number (A-4), a casual one does
         *     not.
         *
         *     A native PostgreSQL enum on the column (DB-15): closed, stable, and on
         *     a column every pool query filters, so four bytes beats a string and a
         *     typo cannot become a value no read path knows how to evaluate.
         * @enum {string}
         */
        QueueType: "ranked" | "casual";
        /**
         * RatingsResponse
         * @description Every rating category a public profile reports.
         *
         *     Named fields rather than an open map, so a client knows at compile time
         *     which categories exist and a missing one is impossible. Adding a fourth
         *     (`bullet`, `correspondence` — both real speed classes this endpoint does
         *     not yet report) is an additive change.
         */
        RatingsResponse: {
            classic: components["schemas"]["app__modules__profiles__presentation__schemas__profile__RatingResponse"];
            rapid: components["schemas"]["app__modules__profiles__presentation__schemas__profile__RatingResponse"];
            blitz: components["schemas"]["app__modules__profiles__presentation__schemas__profile__RatingResponse"];
        };
        /** ReadinessResponse */
        ReadinessResponse: {
            /** Status */
            status: string;
            /** Postgres */
            postgres: boolean;
            /** Redis */
            redis: {
                [key: string]: boolean;
            };
        };
        /**
         * RefreshRequest
         * @description The `POST /auth/refresh` body.
         *
         *     The refresh token travels in the body rather than an `Authorization`
         *     header, because it is not a bearer credential for *this* API — it is
         *     a credential redeemable at exactly one endpoint. Putting it in the
         *     header would invite every interceptor and logging middleware written
         *     for access tokens to treat it as one.
         *
         *     Never in a query string: those land in access logs, proxy logs and
         *     browser history, and unlike an access token this one is good for
         *     thirty days.
         *
         *     Inherits `extra="forbid"` from `BaseRequestDTO`, so a client sending
         *     `user_id` alongside its token is rejected rather than ignored.
         * @example {
         *       "refresh_token": "IqL9nQ8vXo2Zc7Rm4tYp1WkE6dGh0BsN3aFj5UvTxCe"
         *     }
         */
        RefreshRequest: {
            /**
             * Refresh Token
             * @description The refresh token returned by `POST /auth/login` or a previous refresh.
             */
            refresh_token: string;
        };
        /**
         * Region
         * @description Where the player is, for the purpose of who they can be paired with.
         *
         *     AD-25 defers multi-region *infrastructure* and says explicitly what
         *     replaces it: "pairing players by geography (a matchmaking policy, not
         *     an infrastructure change) buys more perceived latency improvement than
         *     any replication topology." This is that policy's input, and it is on
         *     the ticket from the first release for that reason — a pool that is not
         *     partitioned by region at entry cannot be partitioned by it later
         *     without re-queueing everybody.
         *
         *     **Reference data wearing an enum's clothes, and knowingly so.** DB-08
         *     puts variants, time controls and locales in a `reference` schema, and a
         *     region belongs there with them. No `reference` schema exists in code
         *     yet, and creating one for a single closed list would be the speculative
         *     generality CLAUDE.md §1.7 rules out. When `reference` arrives this
         *     becomes `reference.region` and the column a foreign key; the values are
         *     chosen to survive that move unchanged.
         *
         *     `GLOBAL` is not a place. It is the answer for a player who has not been
         *     located and for a pool that does not partition, and it is the default
         *     precisely so that an unlocated player is pairable with everybody rather
         *     than with nobody.
         * @enum {string}
         */
        Region: "global" | "europe" | "north_america" | "south_america" | "asia" | "africa" | "oceania";
        /**
         * RegisterRequest
         * @description The `POST /auth/register` body.
         *
         *     `password` is a `SecretStr`. Beyond the `repr` protection that gives
         *     every layer below, it is also what keeps the value out of the
         *     generated OpenAPI *example* and out of FastAPI's own validation error
         *     payloads — which is why the platform's
         *     `_handle_request_validation_error` drops each error's `input` field
         *     (A64-010): between them, there is no path by which a submitted
         *     password reaches a response body or a log line.
         *
         *     Inherits `extra="forbid"` from `BaseRequestDTO`, so a client sending
         *     `password_hash` — trying to choose their own, pre-computed — is
         *     rejected rather than silently ignored.
         * @example {
         *       "display_name": "Player One",
         *       "email": "player@example.com",
         *       "password": "CorrectHorse1!",
         *       "preferred_language": "en",
         *       "timezone": "Europe/London",
         *       "username": "player_one"
         *     }
         */
        RegisterRequest: {
            /** Username */
            username: string;
            /** Email */
            email: string;
            /**
             * Password
             * Format: password
             */
            password: string;
            /** @default en */
            preferred_language: components["schemas"]["Locale"];
            /**
             * Timezone
             * @default UTC
             */
            timezone: string;
            /** Display Name */
            display_name?: string | null;
        };
        /**
         * RegistrationResponse
         * @description One player's entry in one tournament.
         *
         *     Returned by both participant endpoints, so a client that entered and a
         *     client that withdrew read the same shape and branch on `status` rather
         *     than on which call they made.
         */
        RegistrationResponse: {
            /**
             * Tournament Id
             * Format: uuid
             */
            tournament_id: string;
            /**
             * Player Id
             * Format: uuid
             */
            player_id: string;
            /**
             * Status
             * @description registered or withdrawn.
             */
            status: string;
            /**
             * Registered At
             * Format: date-time
             */
            registered_at: string;
            /** Withdrawn At */
            withdrawn_at?: string | null;
            /**
             * Seed Number
             * @description This entrant's seed, once the tournament has been seeded.
             */
            seed_number?: number | null;
            /**
             * Tournament Status
             * @description The tournament's own lifecycle state, so a client needs one call.
             */
            tournament_status: string;
        };
        /**
         * ReplayPlyResponse
         * @description One ply, and the board it produced.
         */
        ReplayPlyResponse: {
            /** Ply Number */
            ply_number: number;
            /** Side */
            side: string;
            /** Path */
            path: string[];
            /** Captured */
            captured: string[];
            /** Promoted To */
            promoted_to: string | null;
            /** Fingerprint */
            fingerprint: string;
            /** Pieces */
            pieces: components["schemas"]["PlacedPieceResponse"][];
            /** Think Time Ms */
            think_time_ms: number | null;
            /** Remaining Clock Ms */
            remaining_clock_ms: number | null;
        };
        /**
         * ResendVerificationRequest
         * @description The `POST /auth/email/resend` body.
         *
         *     Takes an address rather than requiring a bearer token, because the
         *     person who needs this is by definition the one who never received the
         *     first link — and may well never have signed in. Requiring
         *     authentication would make the endpoint useless to exactly its intended
         *     caller.
         *
         *     `EmailField` reuses `users`' validator, so the address is normalised
         *     identically to registration and login. A malformed address is a
         *     field-level 422 and reveals nothing: an address that cannot be valid
         *     cannot belong to anyone.
         * @example {
         *       "email": "player@example.com"
         *     }
         */
        ResendVerificationRequest: {
            /**
             * Email
             * @description The address the account was registered with.
             */
            email: string;
        };
        /**
         * ResetPasswordRequest
         * @description The `POST /auth/password/reset` body.
         *
         *     The token travels in the body rather than a query string, even though
         *     the *link* the person clicks carries it as a query parameter. The link
         *     points at a frontend route; that page reads the parameter, collects the
         *     new password, and posts both here. Query strings land in access logs,
         *     proxy logs and browser history — survivable for a one-hour single-use
         *     token in the user's own browser, and not survivable in the API tier's
         *     logs, where a token that replaces passwords would sit in plaintext
         *     beside the account it belongs to.
         *
         *     **`password` reuses `PasswordField` from the registration schema**,
         *     which is the point rather than a convenience. It is a `SecretStr`
         *     (keeping the value out of reprs, out of the generated OpenAPI example
         *     and out of FastAPI's validation error payloads) and it runs the same
         *     `validate_password` the service and the domain run. A separate password
         *     type here is how a reset endpoint ends up accepting a password
         *     registration would have refused — the platform would then have two
         *     policies, and the weaker one would be reachable by anyone with an
         *     inbox.
         * @example {
         *       "password": "CorrectHorse1!",
         *       "token": "IqL9nQ8vXo2Zc7Rm4tYp1WkE6dGh0BsN3aFj5UvTxCe"
         *     }
         */
        ResetPasswordRequest: {
            /**
             * Token
             * @description The `token` query parameter from the password reset link.
             */
            token: string;
            /**
             * Password
             * Format: password
             * @description The new password. Must meet the password policy.
             */
            password: string;
        };
        /**
         * ResponseMeta
         * @description Carried on every response — the same three-identifier discipline as
         *     the log line that produced it (services.md §8.2), so a player-reported
         *     bug can be traced from the response they saw straight to the backend
         *     logs that explain it.
         */
        ResponseMeta: {
            /** Request Id */
            request_id?: string | null;
            /** Correlation Id */
            correlation_id?: string | null;
        };
        /** RoundResponse */
        RoundResponse: {
            /** Round Number */
            round_number: number;
            /** Status */
            status: string;
            /** Nodes */
            nodes: components["schemas"]["BracketNodeResponse"][];
        };
        /**
         * SendFriendRequestRequest
         * @description The `POST /friends/requests` body.
         *
         *     One field, and it is a `player_id` rather than a username. Both were
         *     plausible; the id wins because it is what a client already holds
         *     everywhere it would send this from — a search result, a profile page, a
         *     match card all carry `id`, and DM-06 makes it the only reference that
         *     crosses a context boundary. Resolving a username here would add a lookup
         *     whose *timing* answers "does this handle exist" on an authenticated
         *     write endpoint.
         * @example {
         *       "player_id": "019fb9ea-0a0c-7cec-9c5f-402727c31a96"
         *     }
         */
        SendFriendRequestRequest: {
            /**
             * Player Id
             * Format: uuid
             * @description The player to send a request to — the `id` from a search result or a profile. Sending to yourself is a `422`; sending to somebody who already has a pending request from you, or who has one pending to you, is a `409` whose `code` says which.
             * @example 019fb9ea-0a0c-7cec-9c5f-402727c31a96
             */
            player_id: string;
        };
        /**
         * SpeedClass
         * @description How fast a game is, as a rating and leaderboard grouping.
         *
         *     The five names database.md §6.2 already uses for `reference.time_control`
         *     — adopted rather than reinvented, so the day that catalogue ships there
         *     is one vocabulary rather than two that have to be mapped.
         *
         *     A native PostgreSQL enum on the column (DB-15): closed, stable, and on a
         *     column every rating and leaderboard query filters.
         * @enum {string}
         */
        SpeedClass: "bullet" | "blitz" | "rapid" | "classical" | "correspondence";
        /**
         * StandingResponse
         * @description One entrant's final result — §11.
         *
         *     Ranks are **not dense**: two players knocked out in the same round share
         *     one, so an eight-player bracket has no fourth place. That is the
         *     placement rule rather than a gap, and a client that renumbered would be
         *     publishing a comparison nobody made.
         */
        StandingResponse: {
            /**
             * Player Id
             * Format: uuid
             */
            player_id: string;
            /** Final Rank */
            final_rank: number;
            /** Seed Number */
            seed_number: number;
            /** Wins */
            wins: number;
            /** Losses */
            losses: number;
            /** Draws */
            draws: number;
            /**
             * Adjudicated Advancements
             * @description Rounds advanced without a game — a seed tie-break or a no-show.
             */
            adjudicated_advancements: number;
            /** Final Status */
            final_status: string;
            /** Elimination Round */
            elimination_round?: number | null;
            /** Eliminated By Player Id */
            eliminated_by_player_id?: string | null;
        };
        /**
         * StandingsResponse
         * @description A completed tournament's placement, in published order.
         *
         *     Empty while the tournament is still being played — standings are
         *     materialised at completion (§6f), and nothing derives a partial one.
         */
        StandingsResponse: {
            /**
             * Tournament Id
             * Format: uuid
             */
            tournament_id: string;
            /** Standings */
            standings: components["schemas"]["StandingResponse"][];
        };
        /**
         * StatisticsResponse
         * @description A player's aggregate competitive record, across every category.
         *
         *     Rendered wherever a record is shown: on `GET /profiles/{username}` for
         *     a player who has not hidden it, and on `GET /profile/me` always. One
         *     schema for both, so the two views cannot drift into reporting different
         *     fields for the same numbers.
         *
         *     A64-012.6 widened this from four counts to nine fields. Every addition
         *     is a value the `statistics` projection stores; nothing here is computed
         *     by this module and nothing is computed by `profiles`.
         *
         *     `.of()` below is the single mapping. It takes the published
         *     `PlayerStatistics` and names every field, so a field added to that
         *     value object never reaches an anonymous response by accident.
         */
        StatisticsResponse: {
            /**
             * Games Played
             * @description Matches finished, rated and unrated, in every category.
             * @example 0
             */
            games_played: number;
            /**
             * Wins
             * @description Matches won.
             * @example 0
             */
            wins: number;
            /**
             * Losses
             * @description Matches lost.
             * @example 0
             */
            losses: number;
            /**
             * Draws
             * @description Matches drawn.
             * @example 0
             */
            draws: number;
            /**
             * Win Rate
             * @description `wins / games_played`, in `[0, 1]`, to four decimal places. **Draws are in the denominator**, so this is the proportion of games won rather than a chess score percentage — a player with 40 wins, 40 draws and 20 losses has a `win_rate` of `0.4`. `0.0` for a player who has finished no matches. Derived on read, never stored — a stored copy can disagree with the counts printed beside it.
             * @example 0
             */
            win_rate: number;
            /**
             * Current Rating
             * @description The player's headline rating. **Read it together with the `ratings` block**, which reports the same player per speed category and marks a provisional rating as provisional; this single number carries no such marker yet because nothing computes it. Both start at 1500 for a player who has never played.
             * @example 1500
             */
            current_rating: number;
            /**
             * Highest Rating
             * @description The highest rating this player has ever held. Never below `current_rating`. Equal to it for a player who has never played.
             * @example 1500
             */
            highest_rating: number;
            /**
             * Current Streak
             * @description The active run, **signed**: positive counts consecutive wins, negative counts consecutive losses, `0` means the last finished match was a draw or there is no history. One number rather than a length plus a kind, so the two can never disagree.
             * @example 0
             */
            current_streak: number;
            /**
             * Best Win Streak
             * @description The longest run of consecutive wins this player has ever had. Never negative — there is no losing equivalent.
             * @example 0
             */
            best_win_streak: number;
        };
        /**
         * TokenPair
         * @description What a successful sign-in or refresh returns.
         *
         *     The field names are RFC 6749 §5.1's (`access_token`, `token_type`,
         *     `expires_in`) rather than this platform's usual style, on purpose:
         *     every OAuth-aware HTTP client, SDK and debugging tool already knows
         *     them, and inventing `accessToken`/`ttl` would buy consistency with our
         *     own naming at the cost of interoperability with everything else.
         *
         *     `expires_in` is the **access** token's remaining seconds, not the
         *     refresh token's. That is what the RFC specifies and what a client
         *     schedules its refresh against; the refresh token's own 30-day window
         *     is deliberately not published, because a client cannot act on it —
         *     when the refresh fails it must sign in again regardless.
         * @example {
         *       "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMTlmYjllYS0wYTBjLTdjZWMtOWM1Zi00MDI3MjdjMzFhOTYiLCJ0eXBlIjoiYWNjZXNzIn0.signature",
         *       "expires_in": 900,
         *       "refresh_token": "IqL9nQ8vXo2Zc7Rm4tYp1WkE6dGh0BsN3aFj5UvTxCe",
         *       "token_type": "Bearer"
         *     }
         */
        TokenPair: {
            /**
             * Access Token
             * @description A short-lived JWT. Send it as `Authorization: Bearer <token>`.
             */
            access_token: string;
            /**
             * Refresh Token
             * @description An opaque, single-use token for `POST /auth/refresh`. Store it where scripts cannot reach it — it is rotated on every use, and presenting a rotated one revokes the entire session chain.
             */
            refresh_token: string;
            /**
             * Token Type
             * @description Always `Bearer` — RFC 6750's scheme name.
             * @default Bearer
             */
            token_type: string;
            /**
             * Expires In
             * @description Seconds until the **access** token expires, not the refresh token.
             */
            expires_in: number;
        };
        /**
         * TournamentFormat
         * @description How a tournament pairs and advances.
         *
         *     Every member exists from day one for the reason `MatchOrigin` and
         *     `TerminationReason` do (R-19): a format added after tournaments have
         *     been recorded makes every historical query about format wrong. What
         *     differs is which are *runnable* — see `SUPPORTED_FORMATS`.
         * @enum {string}
         */
        TournamentFormat: "single_elimination" | "double_elimination" | "swiss" | "round_robin" | "arena";
        /**
         * TournamentListResponse
         * @description One page of the lobby, newest first — A64-020.0B.
         *
         *     Entries are `TournamentResponse`, the **same** shape the detail endpoint
         *     returns, rather than a slimmer list-only variant. One mapper means a
         *     field cannot be published on one surface and withheld on the other, and
         *     a client can render a lobby card and a detail page from one type.
         */
        TournamentListResponse: {
            /** Entries */
            entries: components["schemas"]["TournamentResponse"][];
            /**
             * Next Cursor
             * @description Opaque; send it back unread for the next page.
             */
            next_cursor?: string | null;
        };
        /**
         * TournamentResponse
         * @description One tournament's public detail — §9.
         *
         *     `created_by` is deliberately absent: who opened a tournament is
         *     operational, and publishing it would leak which tournaments the platform
         *     ran itself.
         */
        TournamentResponse: {
            /**
             * Id
             * Format: uuid
             */
            id: string;
            /** Name */
            name: string;
            /** Format */
            format: string;
            /** Variant */
            variant: string;
            /** Speed Class */
            speed_class: string;
            /** Rated */
            rated: boolean;
            /** Capacity */
            capacity: number;
            /** Status */
            status: string;
            /**
             * Entrant Count
             * @description Live registrations.
             */
            entrant_count: number;
            /**
             * Current Round
             * @description The round being played, or null.
             */
            current_round?: number | null;
            /**
             * Registration Deadline
             * @description When entries close on their own, or null for operator-closed.
             */
            registration_deadline?: string | null;
            /**
             * Created At
             * Format: date-time
             */
            created_at: string;
            /** Started At */
            started_at?: string | null;
            /** Completed At */
            completed_at?: string | null;
        };
        /**
         * TournamentStatus
         * @description Where a tournament is in its life.
         * @enum {string}
         */
        TournamentStatus: "draft" | "registration_open" | "registration_closed" | "in_progress" | "completed" | "cancelled";
        /**
         * UserRead
         * @description The full view of a user's own account.
         *
         *     Includes `email`, which is the account holder's own address. There is
         *     no authorisation layer yet to distinguish "my profile" from "someone
         *     else's", so this shape is currently returned for *any* user id — a gap
         *     A64-011.2 must close by splitting a self view from a public one. It is
         *     recorded here as well as in the router because this is the type that
         *     would carry the leak.
         */
        UserRead: {
            /**
             * Id
             * Format: uuid
             */
            id: string;
            /** Username */
            username: string;
            /** Email */
            email: string;
            /** Display Name */
            display_name: string | null;
            /** Bio */
            bio: string | null;
            /** Country */
            country: string | null;
            avatar: components["schemas"]["AvatarReference"];
            preferred_language: components["schemas"]["Locale"];
            /** Timezone */
            timezone: string;
            /** Is Active */
            is_active: boolean;
            /** Is Verified */
            is_verified: boolean;
            /**
             * Created At
             * Format: date-time
             */
            created_at: string;
            /** Updated At */
            updated_at: string | null;
        };
        /** ValidationError */
        ValidationError: {
            /** Location */
            loc: (string | number)[];
            /** Message */
            msg: string;
            /** Error Type */
            type: string;
            /** Input */
            input?: unknown;
            /** Context */
            ctx?: Record<string, never>;
        };
        /**
         * VerificationAccepted
         * @description The `POST /auth/email/resend` reply.
         *
         *     **Identical whatever happened.** Unknown address, already-verified
         *     account, or a link genuinely sent — the body does not distinguish
         *     them, because distinguishing them would turn this endpoint into a
         *     membership oracle for any address an attacker cares to submit. See
         *     `EmailVerificationService`.
         *
         *     A body rather than a bare `204`, because a client wants something to
         *     render, and the deliberately vague sentence *is* the product
         *     behaviour: "if an account exists for that address, we have sent a
         *     link."
         */
        VerificationAccepted: {
            /**
             * Detail
             * @description A deliberately non-committal confirmation — see the endpoint docs.
             */
            detail: string;
        };
        /**
         * VerifyEmailRequest
         * @description The `POST /auth/email/verify` body.
         *
         *     The token travels in the body rather than a query string, even though
         *     the *link* the person clicks carries it as a query parameter. The link
         *     points at a frontend route; that page reads the parameter and posts it
         *     here. Query strings land in access logs, proxy logs and browser
         *     history, which is survivable for a 24-hour single-use token in the
         *     user's own browser and is not survivable in the API tier's logs.
         *
         *     Inherits `extra="forbid"`, so a client sending `user_id` alongside the
         *     token is rejected rather than having it ignored.
         * @example {
         *       "token": "IqL9nQ8vXo2Zc7Rm4tYp1WkE6dGh0BsN3aFj5UvTxCe"
         *     }
         */
        VerifyEmailRequest: {
            /**
             * Token
             * @description The `token` query parameter from the verification link.
             */
            token: string;
        };
        /**
         * VisibilityLevel
         * @description Which audience a profile field is published to.
         *
         *     A `StrEnum` so the stored value, the wire value and the Python member
         *     are one string — the same choice `BoardTheme` makes, and for the same
         *     reason: a mapping table between three spellings of one concept is three
         *     places for it to drift.
         *
         *     Ordered widest-first in the source, because that is the order a settings
         *     screen renders them and the order `database.md`'s `users.audience`
         *     declares. The order carries no semantics — there is deliberately no
         *     comparison operator, because "friends is more than nobody" is only true
         *     for *this* field and would be false for a future `hide_from` setting.
         * @enum {string}
         */
        VisibilityLevel: "everyone" | "friends" | "nobody";
        /**
         * WebSocketTicketRead
         * @description A freshly minted, single-use ticket.
         *
         *     Deliberately **not** shaped like `TokenPair`. That schema follows RFC
         *     6749 because every OAuth-aware client already knows its field names;
         *     this is not an OAuth artefact, nothing else speaks its protocol, and
         *     borrowing `expires_in` would suggest a refresh flow that does not exist
         *     — a spent ticket is not refreshed, it is replaced.
         */
        WebSocketTicketRead: {
            /**
             * Ticket
             * @description Present as the `ticket` query parameter on `GET /ws`. Single-use: a second connection needs a second ticket.
             */
            ticket: string;
            /**
             * Expires At
             * Format: date-time
             * @description When redemption stops working. Absolute rather than a duration, so a client that was backgrounded between minting and connecting can tell that its ticket went stale without attempting a socket.
             */
            expires_at: string;
        };
        /**
         * RatingResponse
         * @description One category's rating.
         *
         *     An object rather than a bare integer, and the reason is
         *     domain-model.md PR-6: "provisional ratings are visibly marked
         *     everywhere they appear". A number cannot carry the mark, and adding it
         *     later would change `ratings.classic` from an integer to an object —
         *     breaking every client that had read it. See `domain/ratings.py`.
         */
        app__modules__profiles__presentation__schemas__profile__RatingResponse: {
            /**
             * Rating
             * @description The player's current rating in this category. **Read it together with `is_provisional`** — a provisional rating is a starting value, not a measurement.
             * @example 1500
             */
            rating: number;
            /**
             * Is Provisional
             * @description `true` while the rating is not yet established — a player with too few games in this category. Clients must mark these visibly; an unmarked provisional rating misleads both the opponent and the matchmaker.
             * @example true
             */
            is_provisional: boolean;
            /**
             * Games Played
             * @description Matches that have moved *this* rating. Not the player's total, which is `statistics.games_played` and counts every match including unrated ones.
             * @example 0
             */
            games_played: number;
        };
        /**
         * RatingResponse
         * @description One player's rating in one `(variant, speed_class)` key.
         */
        app__modules__rating__presentation__schemas__ratings__RatingResponse: {
            /** Variant */
            variant: string;
            /** Speed Class */
            speed_class: string;
            /**
             * Rating
             * @description The Glicko-2 rating value.
             */
            rating: number;
            /**
             * Deviation
             * @description How sure the platform is. Higher means less certain.
             */
            deviation: number;
            /** Games Played */
            games_played: number;
            /**
             * Is Provisional
             * @description Fewer rated games than the threshold. Shown, never hidden.
             */
            is_provisional: boolean;
        };
    };
    responses: never;
    parameters: never;
    requestBodies: never;
    headers: never;
    pathItems: never;
}
export type $defs = Record<string, never>;
export interface operations {
    liveness_health_get: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_LivenessResponse_"];
                };
            };
        };
    };
    readiness_health_ready_get: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_ReadinessResponse_"];
                };
            };
        };
    };
    liveness_api_v1_health_get: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_LivenessResponse_"];
                };
            };
        };
    };
    readiness_api_v1_health_ready_get: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_ReadinessResponse_"];
                };
            };
        };
    };
    search_users_api_v1_users_search_get: {
        parameters: {
            query: {
                /** @description What to search for — 2-50 characters, matched against usernames and display names. Surrounding whitespace is trimmed. Case-insensitive, and accent-insensitive for display names. Wildcards are not supported and are rejected. */
                q: string;
                /** @description Results per page. */
                limit?: number;
                /** @description Opaque cursor from a previous page. Pass it back unchanged **with the same `q`** — a cursor is bound to the term it was issued for, and presenting it alongside a different one returns `422` rather than an arbitrary slice. */
                cursor?: string | null;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A ranked page of matching players. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_CursorPage_ProfileResponse__"];
                };
            };
            /** @description No access token was presented, or it was invalid or expired. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description The search term is empty, shorter than 2 characters, longer than 50, carries a wildcard, or contains no letter or digit — or the cursor is malformed or belongs to a different term. `message` says which. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Too many searches from this account. Counted **per user**, not per network address, so a shared connection is never somebody else's problem. `Retry-After` says how long to wait. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    get_user_api_v1_users__user_id__get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                user_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The player's handle and avatar. Nothing else. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_PublicUserResponse_"];
                };
            };
            /** @description No account has that identifier. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description A path or query parameter was malformed — an identifier that is not a UUID, or a `limit` outside 1-100. `message` names which. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    list_users_api_v1_users_get: {
        parameters: {
            query?: {
                /** @description Opaque cursor from a previous page. */
                cursor?: string | null;
                /** @description Rows per page. */
                limit?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of public player identities, newest cursor last. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_CursorPage_PublicUserResponse__"];
                };
            };
            /** @description A path or query parameter was malformed — an identifier that is not a UUID, or a `limit` outside 1-100. `message` names which. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    register_api_v1_auth_register_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["RegisterRequest"];
            };
        };
        responses: {
            /** @description The newly created account. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_UserRead_"];
                };
            };
            /** @description The username or email address is already registered. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description A field failed validation. `code` names which one. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description A rate limit was exceeded. `Retry-After` gives the number of seconds to wait; `X-RateLimit-Limit`, `X-RateLimit-Remaining` and `X-RateLimit-Reset` describe the limit that bound. The same three `X-RateLimit-*` headers are returned on successful responses, so a client can pace itself before being refused. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    login_api_v1_auth_login_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["LoginRequest"];
            };
        };
        responses: {
            /** @description An access token and a refresh token. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_TokenPair_"];
                };
            };
            /** @description The credential was missing, malformed, expired or revoked. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Credentials were correct but the account may not sign in. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description A field failed validation. `code` names which one. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description A rate limit was exceeded. `Retry-After` gives the number of seconds to wait; `X-RateLimit-Limit`, `X-RateLimit-Remaining` and `X-RateLimit-Reset` describe the limit that bound. The same three `X-RateLimit-*` headers are returned on successful responses, so a client can pace itself before being refused. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    refresh_api_v1_auth_refresh_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["RefreshRequest"];
            };
        };
        responses: {
            /** @description A new access token and a rotated refresh token. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_TokenPair_"];
                };
            };
            /** @description The credential was missing, malformed, expired or revoked. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description A field failed validation. `code` names which one. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description A rate limit was exceeded. `Retry-After` gives the number of seconds to wait; `X-RateLimit-Limit`, `X-RateLimit-Remaining` and `X-RateLimit-Reset` describe the limit that bound. The same three `X-RateLimit-*` headers are returned on successful responses, so a client can pace itself before being refused. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    logout_api_v1_auth_logout_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["RefreshRequest"];
            };
        };
        responses: {
            /** @description The session was revoked, or was already revoked. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The credential was missing, malformed, expired or revoked. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description A field failed validation. `code` names which one. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    logout_all_api_v1_auth_logout_all_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Every session for the account was revoked. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The credential was missing, malformed, expired or revoked. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    websocket_ticket_api_v1_auth_ws_ticket_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A ticket valid for seconds, redeemable once. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_WebSocketTicketRead_"];
                };
            };
            /** @description The credential was missing, malformed, expired or revoked. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    me_api_v1_auth_me_get: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The caller's own account. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_UserRead_"];
                };
            };
            /** @description The credential was missing, malformed, expired or revoked. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description The account no longer exists — deleted while a valid token was in flight. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    verify_email_api_v1_auth_email_verify_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["VerifyEmailRequest"];
            };
        };
        responses: {
            /** @description The account, now verified. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_UserRead_"];
                };
            };
            /** @description A field failed validation. `code` names which one. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    resend_verification_api_v1_auth_email_resend_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ResendVerificationRequest"];
            };
        };
        responses: {
            /** @description Accepted. The reply is identical whether or not an account exists. */
            202: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_VerificationAccepted_"];
                };
            };
            /** @description A field failed validation. `code` names which one. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description A rate limit was exceeded. `Retry-After` gives the number of seconds to wait; `X-RateLimit-Limit`, `X-RateLimit-Remaining` and `X-RateLimit-Reset` describe the limit that bound. The same three `X-RateLimit-*` headers are returned on successful responses, so a client can pace itself before being refused. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    forgot_password_api_v1_auth_password_forgot_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ForgotPasswordRequest"];
            };
        };
        responses: {
            /** @description Accepted. The reply is identical whether or not an account exists. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description A field failed validation. `code` names which one. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description A rate limit was exceeded. `Retry-After` gives the number of seconds to wait; `X-RateLimit-Limit`, `X-RateLimit-Remaining` and `X-RateLimit-Reset` describe the limit that bound. The same three `X-RateLimit-*` headers are returned on successful responses, so a client can pace itself before being refused. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    reset_password_api_v1_auth_password_reset_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ResetPasswordRequest"];
            };
        };
        responses: {
            /** @description The password was replaced and every session was revoked. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description A field failed validation. `code` names which one. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description A rate limit was exceeded. `Retry-After` gives the number of seconds to wait; `X-RateLimit-Limit`, `X-RateLimit-Remaining` and `X-RateLimit-Reset` describe the limit that bound. The same three `X-RateLimit-*` headers are returned on successful responses, so a client can pace itself before being refused. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    get_profile_api_v1_profiles__username__get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The player's handle. Matching is case-insensitive. */
                username: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The player's public profile. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_ProfileResponse_"];
                };
            };
            /** @description No visible profile for that username. Returned identically whether the username was never registered or belongs to a deactivated account. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description The username is not a possible handle — wrong length, or characters a username may not contain. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Too many profile reads from this address. Counted **per network address**, because this endpoint is anonymous and there is no account to count. `Retry-After` says how long to wait. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    get_my_profile_api_v1_profile_me_get: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Your profile, including fields the public view omits. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_MyProfileResponse_"];
                };
            };
            /** @description No access token was presented, or it was invalid or expired. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description The account no longer exists — deleted while a valid token was in flight. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    update_my_profile_api_v1_profile_patch: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ProfileUpdateRequest"];
            };
        };
        responses: {
            /** @description The profile as it now stands, with normalised values. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_MyProfileResponse_"];
                };
            };
            /** @description No access token was presented, or it was invalid or expired. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description The account no longer exists — deleted while a valid token was in flight. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description A field failed validation, or the body carried a field this endpoint does not accept. `message` names which. Unknown fields are **rejected**, not ignored — a silently dropped `username` would look like a successful rename. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    get_my_privacy_settings_api_v1_profile_privacy_get: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description All five flags, as they currently stand. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_PrivacySettingsResponse_"];
                };
            };
            /** @description No access token was presented, or it was invalid or expired. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description The account no longer exists — deleted while a valid token was in flight. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    update_my_privacy_settings_api_v1_profile_privacy_patch: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["PrivacySettingsUpdateRequest"];
            };
        };
        responses: {
            /** @description All five flags as they now stand. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_PrivacySettingsResponse_"];
                };
            };
            /** @description No access token was presented, or it was invalid or expired. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description The account no longer exists — deleted while a valid token was in flight. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description The body carried a field this endpoint does not accept, or a flag was sent as `null`. `message` names which. Unknown fields are **rejected**, not ignored — a client that believed it had hidden something it had not would act as though the field were private. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Too many privacy updates from this account. Counted **per user**, not per network address, so a shared connection is never somebody else's problem. `Retry-After` says how long to wait. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    get_my_preferences_api_v1_profile_preferences_get: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Every preference group, complete. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_PreferencesResponse_"];
                };
            };
            /** @description No access token was presented, or it was invalid or expired. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description The account no longer exists — deleted while a valid token was in flight. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    update_my_preferences_api_v1_profile_preferences_patch: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["PreferencesUpdateRequest"];
            };
        };
        responses: {
            /** @description Every preference group as it now stands. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_PreferencesResponse_"];
                };
            };
            /** @description No access token was presented, or it was invalid or expired. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description The account no longer exists — deleted while a valid token was in flight. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description An unknown preference group or key, a value outside its allowed set, a timezone this system does not know, or a key sent as `null`. `message` names which. Unknown keys are **rejected** at both levels — an unknown group and an unknown setting inside a known group are both errors. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Too many preference updates from this account. Counted **per user**, not per network address, so a shared connection is never somebody else's problem. `Retry-After` says how long to wait. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    get_avatar_api_v1_profile_avatar_get: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The current avatar's URLs, version and upload time. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_AvatarResponse_"];
                };
            };
            /** @description No access token was presented, or it was invalid or expired. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description This account has no avatar. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    upload_avatar_api_v1_profile_avatar_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "multipart/form-data": components["schemas"]["Body_upload_avatar_api_v1_profile_avatar_post"];
            };
        };
        responses: {
            /** @description The avatar as it now stands, with its stored dimensions. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_AvatarResponse_"];
                };
            };
            /** @description No access token was presented, or it was invalid or expired. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description The upload was rejected. `code` is `avatar_too_large` when the file exceeds 5 MB — a client can re-encode and retry — and `validation_error` when the file is empty, is not one of the accepted formats, or is not a readable image. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Too many avatar uploads from this account. Counted **per user**, not per network address, so a shared connection is never somebody else's problem. `Retry-After` says how long to wait. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    delete_avatar_api_v1_profile_avatar_delete: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The cleared avatar state, with its new version. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_AvatarResponse_"];
                };
            };
            /** @description No access token was presented, or it was invalid or expired. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    send_friend_request_api_v1_friends_requests_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["SendFriendRequestRequest"];
            };
        };
        responses: {
            /** @description The request as created, with the recipient's public profile. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_FriendRequestResponse_"];
                };
            };
            /** @description No access token was presented, or it was invalid or expired. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description The request cannot be created or resolved in its current state. `code` says which: `duplicate_friend_request` (you already have one pending to that player), `opposite_friend_request_pending` (they have one pending to you — respond to it instead), or `conflict` (this request has already been resolved, possibly on another device). */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description The body or a query parameter failed validation. `message` names which. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Too many friend-request actions from this account. Counted **per user**, not per network address, so a shared connection is never somebody else's problem. `Retry-After` says how long to wait. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    list_incoming_api_v1_friends_requests_incoming_get: {
        parameters: {
            query?: {
                /** @description Requests per page. */
                limit?: number;
                /** @description Opaque cursor from a previous page. Pass it back unchanged. */
                cursor?: string | null;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of pending incoming requests, newest first. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_CursorPage_FriendRequestResponse__"];
                };
            };
            /** @description No access token was presented, or it was invalid or expired. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description The body or a query parameter failed validation. `message` names which. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    list_outgoing_api_v1_friends_requests_outgoing_get: {
        parameters: {
            query?: {
                /** @description Requests per page. */
                limit?: number;
                /** @description Opaque cursor from a previous page. Pass it back unchanged. */
                cursor?: string | null;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of pending outgoing requests, newest first. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_CursorPage_FriendRequestResponse__"];
                };
            };
            /** @description No access token was presented, or it was invalid or expired. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description The body or a query parameter failed validation. `message` names which. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    accept_friend_request_api_v1_friends_requests__request_id__accept_post: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The request identifier. */
                request_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The request in its resolved state. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_FriendRequestResponse_"];
                };
            };
            /** @description No access token was presented, or it was invalid or expired. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description You are not the party entitled to this action — only the recipient may accept or decline, and only the sender may cancel. The message never names the other player. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description No friend request with that identifier. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description The request cannot be created or resolved in its current state. `code` says which: `duplicate_friend_request` (you already have one pending to that player), `opposite_friend_request_pending` (they have one pending to you — respond to it instead), or `conflict` (this request has already been resolved, possibly on another device). */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
            /** @description Too many friend-request actions from this account. Counted **per user**, not per network address, so a shared connection is never somebody else's problem. `Retry-After` says how long to wait. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    decline_friend_request_api_v1_friends_requests__request_id__decline_post: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The request identifier. */
                request_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The request in its resolved state. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_FriendRequestResponse_"];
                };
            };
            /** @description No access token was presented, or it was invalid or expired. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description You are not the party entitled to this action — only the recipient may accept or decline, and only the sender may cancel. The message never names the other player. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description No friend request with that identifier. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description The request cannot be created or resolved in its current state. `code` says which: `duplicate_friend_request` (you already have one pending to that player), `opposite_friend_request_pending` (they have one pending to you — respond to it instead), or `conflict` (this request has already been resolved, possibly on another device). */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
            /** @description Too many friend-request actions from this account. Counted **per user**, not per network address, so a shared connection is never somebody else's problem. `Retry-After` says how long to wait. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    cancel_friend_request_api_v1_friends_requests__request_id__delete: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The request identifier. */
                request_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The request in its resolved state. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_FriendRequestResponse_"];
                };
            };
            /** @description No access token was presented, or it was invalid or expired. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description You are not the party entitled to this action — only the recipient may accept or decline, and only the sender may cancel. The message never names the other player. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description No friend request with that identifier. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description The request cannot be created or resolved in its current state. `code` says which: `duplicate_friend_request` (you already have one pending to that player), `opposite_friend_request_pending` (they have one pending to you — respond to it instead), or `conflict` (this request has already been resolved, possibly on another device). */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
            /** @description Too many friend-request actions from this account. Counted **per user**, not per network address, so a shared connection is never somebody else's problem. `Retry-After` says how long to wait. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    count_friends_api_v1_friends_count_get: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description How many friends you currently have. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_FriendCountResponse_"];
                };
            };
            /** @description No access token was presented, or it was invalid or expired. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    list_friends_api_v1_friends_get: {
        parameters: {
            query?: {
                /** @description Friends per page. */
                limit?: number;
                /** @description Opaque cursor from a previous page. Pass it back unchanged. */
                cursor?: string | null;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of your friends, most recently added first. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_CursorPage_FriendResponse__"];
                };
            };
            /** @description No access token was presented, or it was invalid or expired. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description The body or a query parameter failed validation. `message` names which. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    get_friendship_api_v1_friends__player_id__get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The friend to inspect. */
                player_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The friendship, with the other player's profile. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_FriendshipDetailsResponse_"];
                };
            };
            /** @description No access token was presented, or it was invalid or expired. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description You are not friends with that player. Returned identically whether you never were or the friendship has already ended — which of the two applies is not something an endpoint should answer. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    remove_friend_api_v1_friends__player_id__delete: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The friend to remove. */
                player_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The friendship has ended. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No access token was presented, or it was invalid or expired. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
            /** @description Too many friend-request actions from this account. Counted **per user**, not per network address, so a shared connection is never somebody else's problem. `Retry-After` says how long to wait. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    list_blocked_players_api_v1_blocks_get: {
        parameters: {
            query?: {
                /** @description Blocked players per page. */
                limit?: number;
                /** @description Opaque cursor from a previous page. Pass it back unchanged. */
                cursor?: string | null;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of blocked players, most recently blocked first. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_CursorPage_BlockedPlayerResponse__"];
                };
            };
            /** @description No access token was presented, or it was invalid or expired. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description The body or a query parameter failed validation. `message` names which. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    block_player_api_v1_blocks_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["BlockPlayerRequest"];
            };
        };
        responses: {
            /** @description The block as created, with the blocked player's profile. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_BlockedPlayerResponse_"];
                };
            };
            /** @description No access token was presented, or it was invalid or expired. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description You have already blocked that player. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description You cannot block yourself. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Too many friend-request actions from this account. Counted **per user**, not per network address, so a shared connection is never somebody else's problem. `Retry-After` says how long to wait. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    unblock_player_api_v1_blocks__player_id__delete: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The player to unblock. */
                player_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The block has been lifted. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No access token was presented, or it was invalid or expired. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
            /** @description Too many friend-request actions from this account. Counted **per user**, not per network address, so a shared connection is never somebody else's problem. `Retry-After` says how long to wait. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    join_queue_api_v1_matchmaking_queue_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["JoinQueueRequest"];
            };
        };
        responses: {
            /** @description The ticket as created, with the pool's current depth. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_QueueTicketResponse_"];
                };
            };
            /** @description No access token was presented, or it was invalid or expired. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /**
             * @description You already hold a live queue ticket, **or** you declined a match recently and are in a short cooldown. The `code` distinguishes them: `conflict` means leave the queue you are in; `queue_cooldown_active` means wait, and `Retry-After` says how long.
             *
             *     One ticket per player, **across all pools** — leave the queue before joining a different one.
             */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description The body failed validation, or you are recorded as signed out. `message` says which. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Too many queue actions from this account. Counted **per user**, not per network address, so a shared connection is never somebody else's problem. `Retry-After` says how long to wait. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    leave_queue_api_v1_matchmaking_queue_delete: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description You are no longer queued. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No access token was presented, or it was invalid or expired. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Too many queue actions from this account. Counted **per user**, not per network address, so a shared connection is never somebody else's problem. `Retry-After` says how long to wait. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    read_my_ticket_api_v1_matchmaking_queue_me_get: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Your live ticket, with the pool's current depth. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_QueueTicketResponse_"];
                };
            };
            /** @description No access token was presented, or it was invalid or expired. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description You are not currently in a queue. Returned identically whether you never joined, left, were matched, or your ticket expired. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    read_pending_match_api_v1_matchmaking_matches_pending_get: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Your pending match, with a preview of your opponent. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_PendingMatchResponse_"];
                };
            };
            /** @description No access token was presented, or it was invalid or expired. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description No match is waiting for your answer. Returned identically whether you were never paired, already answered, or the offer expired. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    accept_match_api_v1_matchmaking_matches__match_id__accept_post: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The match you were offered. */
                match_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The match, including whether your opponent has answered. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_PendingMatchResponse_"];
                };
            };
            /** @description No access token was presented, or it was invalid or expired. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description No such match, **or** it is not yours. The two are deliberately indistinguishable — a different status for somebody else's match would make live match identifiers enumerable. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description That match is no longer awaiting your answer — your opponent declined, the window closed, or you both already accepted. Read `GET /matchmaking/matches/pending` to see where things stand. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
            /** @description Too many queue actions from this account. Counted **per user**, not per network address, so a shared connection is never somebody else's problem. `Retry-After` says how long to wait. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    decline_match_api_v1_matchmaking_matches__match_id__decline_post: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The match you were offered. */
                match_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The match, now cancelled. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_PendingMatchResponse_"];
                };
            };
            /** @description No access token was presented, or it was invalid or expired. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description No such match, **or** it is not yours. The two are deliberately indistinguishable — a different status for somebody else's match would make live match identifiers enumerable. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description That match is no longer awaiting your answer — your opponent declined, the window closed, or you both already accepted. Read `GET /matchmaking/matches/pending` to see where things stand. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
            /** @description Too many queue actions from this account. Counted **per user**, not per network address, so a shared connection is never somebody else's problem. `Retry-After` says how long to wait. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    player_match_history_api_v1_players__player_id__matches_get: {
        parameters: {
            query?: {
                /** @description An opaque cursor from a previous page. */
                after?: string | null;
                limit?: number;
            };
            header?: never;
            path: {
                /** @description Whose history to read. */
                player_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_MatchHistoryResponse_"];
                };
            };
            /** @description The pagination cursor is not valid */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    match_replay_api_v1_matches__match_id__replay_get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Which match to replay. */
                match_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_MatchReplayResponse_"];
                };
            };
            /** @description No such match, or it is not visible to you */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description This match was played under rules this build cannot reproduce */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    my_ratings_api_v1_ratings_me_get: {
        parameters: {
            query?: {
                /** @description Which rule set. */
                variant?: components["schemas"]["ProductVariant"];
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_PlayerRatingsResponse_"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    player_ratings_api_v1_players__player_id__ratings_get: {
        parameters: {
            query?: {
                /** @description Which rule set. */
                variant?: components["schemas"]["ProductVariant"];
            };
            header?: never;
            path: {
                /** @description Whose ratings to read. */
                player_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_PlayerRatingsResponse_"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    leaderboard_api_v1_leaderboard_get: {
        parameters: {
            query?: {
                /** @description Which rule set. */
                variant?: components["schemas"]["ProductVariant"];
                /** @description Which speed class. */
                speed_class?: components["schemas"]["SpeedClass"];
                /** @description An opaque cursor from a previous page. */
                after?: string | null;
                limit?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_LeaderboardResponse_"];
                };
            };
            /** @description The pagination cursor is not valid */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    leaderboard_around_api_v1_leaderboard_around__player_id__get: {
        parameters: {
            query?: {
                /** @description Which rule set. */
                variant?: components["schemas"]["ProductVariant"];
                /** @description Which speed class. */
                speed_class?: components["schemas"]["SpeedClass"];
                span?: number;
            };
            header?: never;
            path: {
                /** @description Whose position to find. */
                player_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_LeaderboardNeighbourhoodResponse_"];
                };
            };
            /** @description This player has no rating in that key */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    tournament_lobby_api_v1_tournaments_get: {
        parameters: {
            query?: {
                /** @description Only this lifecycle state. */
                status?: components["schemas"]["TournamentStatus"] | null;
                /** @description Only this tournament format. */
                format?: components["schemas"]["TournamentFormat"] | null;
                /** @description Only this rule set. */
                variant?: components["schemas"]["ProductVariant"] | null;
                /** @description Only this speed class. */
                speed_class?: components["schemas"]["SpeedClass"] | null;
                /** @description Only rated, or only casual. */
                rated?: boolean | null;
                /** @description An opaque cursor from a previous page. */
                after?: string | null;
                limit?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_TournamentListResponse_"];
                };
            };
            /** @description The pagination cursor is not valid */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    tournament_detail_api_v1_tournaments__tournament_id__get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Which tournament to read. */
                tournament_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_TournamentResponse_"];
                };
            };
            /** @description No such tournament */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    tournament_bracket_api_v1_tournaments__tournament_id__bracket_get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Which tournament's bracket to read. */
                tournament_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_BracketResponse_"];
                };
            };
            /** @description No such tournament */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    tournament_standings_api_v1_tournaments__tournament_id__standings_get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Which tournament's results to read. */
                tournament_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_StandingsResponse_"];
                };
            };
            /** @description No such tournament */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    enter_tournament_api_v1_tournaments__tournament_id__registrations_post: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Which tournament to enter. */
                tournament_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_RegistrationResponse_"];
                };
            };
            /** @description No such tournament, or no such player */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Registration is closed, the deadline passed, the field is full, or you already entered */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    withdraw_from_tournament_api_v1_tournaments__tournament_id__registrations_me_delete: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Which tournament to leave. */
                tournament_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_RegistrationResponse_"];
                };
            };
            /** @description No such tournament, or you have no live entry */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Registration has closed and the field is fixed */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    player_tournaments_api_v1_players__player_id__tournaments_get: {
        parameters: {
            query?: {
                /** @description An opaque cursor from a previous page. */
                after?: string | null;
                limit?: number;
            };
            header?: never;
            path: {
                /** @description Whose participation to read. */
                player_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiResponse_PlayerTournamentsResponse_"];
                };
            };
            /** @description The pagination cursor is not valid */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
}
