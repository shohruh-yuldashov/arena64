"""The repository foundation — database-only CRUD, pagination, and
filtering mechanics shared across future modules' concrete repository
implementations.

Read `app.repositories.base`'s docstring before using any of this: it
explains precisely how this reconciles with repositories.md §10's warning
against a generic `BaseRepository`, which this package is named after on
purpose and is not exempt from by accident.
"""
